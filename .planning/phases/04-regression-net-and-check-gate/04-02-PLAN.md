---
phase: 04-regression-net-and-check-gate
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - DESCRIPTION
  - NAMESPACE
  - R/EventStudy-package.R
  - NEWS.md
  - cran-comments.md
autonomous: true
requirements: [TEST-03, TEST-04]
estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The full existing test suite (~1291 pass / 0 fail / ~26 skip) runs green with no regressions (TEST-03)"
    - "devtools::document() regenerates NAMESPACE/Rd cleanly with no uncommitted drift (TEST-04)"
    - "R CMD check (or documented graceful fallback) introduces no NEW NOTEs/WARNINGs vs the recorded pre-milestone baseline (TEST-04)"
    - "any new check finding the milestone caused is fixed at its source (globalVariables/DESCRIPTION Suggests/Rd), not suppressed"
  artifacts:
    - "R/EventStudy-package.R (globalVariables updated if new NSE columns)"
    - "DESCRIPTION (Suggests reconciled for withr/quadprog/callr as actually used)"
    - "NEWS.md (baseline record + check-gate result)"
  key_links:
    - "recorded pre-milestone baseline NOTEs/WARNINGs is the comparison anchor — the gate is delta, not absolute zero"
    - "any check-surfaced defect traces to a real source fix, never a fabricated clean run"
---

<objective>
Close the acceptance gate: run `devtools::document()` then `R CMD check` (via `devtools::check()` or the built tarball), record the pre-milestone baseline of NOTEs/WARNINGs, and assert the milestone introduces NO NEW NOTEs/WARNINGs vs that baseline. Fix any new finding the milestone caused (undeclared globals from new NSE columns, missing Rd for new exports, Suggests for test-only packages) at its source. Confirm the full existing suite stays green.

Purpose: Meet the CRAN bar and prove the regression net does not itself break the package (TEST-03, TEST-04).
Output: possible edits to `R/EventStudy-package.R`, `DESCRIPTION`, `NAMESPACE`, regenerated `man/`, `NEWS.md` baseline record, `cran-comments.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-net-and-check-gate/04-CONTEXT.md

@DESCRIPTION
@R/EventStudy-package.R
@NEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green + document() drift check — the pre-gate baseline of correctness</name>
  <files>NAMESPACE, R/EventStudy-package.R</files>
  <read_first>
    - DESCRIPTION — `Config/roxygen2/version: 8.0.0`, `RoxygenNote` managed by roxygen2. `devtools::document()` regenerates NAMESPACE and man/*.Rd from `@export`/`@param` tags.
    - R/EventStudy-package.R line 2 — `utils::globalVariables(c(...))` holds every NSE column name used in dplyr/ggplot2 pipelines. New derived columns added this milestone (grep the milestone diff for new `mutate(`/`.data$` columns) that are not in this vector will surface as "no visible binding for global variable" NOTEs.
    - The full test suite currently reports ~1291 pass / 0 fail / ~26 skip. This task must confirm that number holds AFTER Plan 01's new test file exists (Plan 01 is a dependency, so test_contract_matrix.R is already present).
  </read_first>
  <action>
    First run the full test suite via `devtools::test()` (or `testthat::test_dir("tests/testthat")`) and confirm 0 failures / 0 unexpected errors — this is the TEST-03 gate and must include Plan 01's new matrix file. If any failure appears, STOP and record it; do not proceed to the check until the suite is green.

    Then run `devtools::document()` and check for git drift in `NAMESPACE` and `man/`. If document() changes those files, it means docs were stale or Plan 01/earlier work added exports without regenerating — commit the regenerated files. If new NSE-column "no visible binding" candidates exist, add the missing column names to the `utils::globalVariables(c(...))` vector in R/EventStudy-package.R (alphabetically near their neighbors, matching the existing snake_case style). Do NOT remove any existing global-variable declarations. Do not add `@export` to anything new — this milestone added no new exports (testing/docs only); if document() wants to ADD an export you did not intend, investigate rather than accept it.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::document(quiet=TRUE); df <- as.data.frame(testthat::test_dir("tests/testthat", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error)); cat("pass=", sum(df$pass), " fail=", sum(df$failed), " skip=", sum(df$skipped), "\n")'</automated>
    <fails_when>Any test fails or errors after document() regenerates docs, or document() is not runnable (roxygen2 absent) with no recorded reason.</fails_when>
  </verify>
  <done>Full suite green (0 fail / 0 error, ~1291 pass, ~26 skip), docs regenerated, any new NSE columns declared in globalVariables, NAMESPACE has no unexpected export additions.</done>
</task>

<task type="auto">
  <name>Task 2: R CMD check gate — record baseline, assert no NEW NOTEs/WARNINGs, fix new ones at source</name>
  <files>DESCRIPTION, R/EventStudy-package.R, NEWS.md, cran-comments.md</files>
  <read_first>
    - CONTEXT.md "R CMD Check Gate" — the gate is NO NEW NOTEs/WARNINGs vs the pre-milestone baseline (commit 63d67a1), NOT absolute zero. Optional-package Suggests NOTEs are expected baseline noise. Record the baseline explicitly.
    - DESCRIPTION Suggests — already includes `withr` (Phase 1), `quadprog`, `did`, `DIDmultiplegt`, `didimputation`, `rugarch`, `rmgarch`, `sandwich`, etc. Verify: (a) `withr` is used in tests (test_contract.R uses `withr::with_options`) → must stay in Suggests; (b) `quadprog` is used by synthetic control → confirm Suggests entry present (it is); (c) `callr` — used ONLY via `requireNamespace()` for the optional DIDmultiplegt subprocess probe. Per CONTEXT.md: decide if it needs a Suggests entry — YES if any test references `callr::` directly, otherwise NO (requireNamespace-only optional use does not require a Suggests entry). Grep the tests for `callr::` before deciding; do not add it speculatively.
    - cran-comments.md — the standard CRAN submission note file; update it with the check result and the baseline-vs-new NOTE summary if it exists (create only if the repo convention expects it — check first).
    - If `R CMD check` / `devtools::check()` cannot fully run in this environment (no LaTeX, offline, no pandoc), CONTEXT.md constraints REQUIRE a graceful fallback: run `devtools::check(document=TRUE, args="--no-manual")` or at minimum `devtools::document()` + a manual NOTE audit (NAMESPACE consistency, undeclared globals via `R CMD check --no-manual`, examples that error), and capture the EXACT limitation in the SUMMARY — never fabricate a clean check.
  </read_first>
  <action>
    Record the pre-milestone baseline: check out the package at commit 63d67a1 in a scratch worktree (or reason from the known baseline set of optional-package Suggests NOTEs) and enumerate its NOTEs/WARNINGs, OR — if a worktree check is impractical in-environment — document the baseline as "the set of Suggests-related and pre-existing NOTEs present at 63d67a1" and capture the current-HEAD full NOTE/WARNING list, then diff the two sets by category. Write the baseline record into NEWS.md (under the milestone section) and/or cran-comments.md so the delta is auditable.

    Run the check with a resilient command that degrades gracefully: attempt `devtools::check(document = TRUE, args = c("--no-manual", "--no-build-vignettes"), error_on = "never")` and capture NOTEs/WARNINGs/ERRORs. Assert: zero ERRORs; and no NEW WARNING or NOTE category that is absent from the baseline. For each NEW finding attributable to this milestone, fix at source:
      - "no visible binding for global variable X" → add X to `utils::globalVariables()` in R/EventStudy-package.R.
      - "undocumented / missing Rd" → this milestone added no exports; if it appears, it is stale docs → re-run document().
      - "package not declared in Suggests but used" → add the package to DESCRIPTION Suggests (only if a test/example actually references it via `::`).
    Do NOT introduce any new package logic and do NOT suppress a finding to make the gate pass — every fix must trace to a real docs/metadata defect (per CONTEXT.md "testing/acceptance only" constraint).

    If the environment cannot run the full check, execute the documented fallback and record in the SUMMARY the exact command run, what could not run and why (e.g. "vignette rebuild skipped: pandoc absent"), and the manual NOTE audit result — so a later full-environment run can confirm.
  </action>
  <verify>
    <automated>Rscript -e 'res <- tryCatch(devtools::check(document = TRUE, args = c("--no-manual", "--no-build-vignettes"), error_on = "never", quiet = TRUE), error = function(e) e); if (inherits(res, "rcmdcheck")) { cat("ERRORS:", length(res$errors), " WARNINGS:", length(res$warnings), " NOTES:", length(res$notes), "\n"); stopifnot(length(res$errors) == 0) } else { cat("CHECK COULD NOT RUN — fallback required:", conditionMessage(res), "\n"); devtools::document(quiet=TRUE) }'</automated>
    <fails_when>R CMD check reports any ERROR, or a new WARNING/NOTE category not present in the recorded 63d67a1 baseline remains unaddressed, or (fallback path) neither the check nor document()+manual-audit ran and the limitation was not captured.</fails_when>
  </verify>
  <done>Baseline recorded; check runs (or documented fallback executed with captured limitation); zero ERRORs; no NEW NOTE/WARNING category vs baseline; any milestone-caused finding fixed at source; NEWS.md/cran-comments.md carry the baseline-vs-result record.</done>
</task>

</tasks>

<threat_model>
No applicable threats. Internal R package, no untrusted-input boundary; this phase edits package metadata/docs and runs the CRAN check only. No new network, filesystem-write, deserialization, or package-install surface is introduced (no new dependencies added beyond reconciling already-used test-only Suggests). Per CONTEXT.md, "no applicable threats" is declared.
</threat_model>

<verification>
- `Rscript -e 'df <- as.data.frame(testthat::test_dir("tests/testthat", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'` — full suite green (TEST-03).
- `devtools::check(document=TRUE, args=c("--no-manual","--no-build-vignettes"), error_on="never")` — zero ERRORs, no new NOTE/WARNING vs baseline (TEST-04); or the documented graceful fallback with captured limitation.
- NEWS.md/cran-comments.md contain the pre-milestone baseline record and the delta result.
</verification>

<success_criteria>
- [ ] Full existing suite (~1291 pass / 0 fail / ~26 skip) green with no regressions (TEST-03).
- [ ] devtools::document() leaves no doc drift; globalVariables updated for any new NSE columns.
- [ ] Pre-milestone baseline (63d67a1) NOTEs/WARNINGs recorded.
- [ ] R CMD check (or documented fallback) shows no NEW NOTEs/WARNINGs vs baseline; zero ERRORs (TEST-04).
- [ ] Every check-surfaced fix traces to a real docs/metadata defect — no new package logic, no suppression.
</success_criteria>

<output>
Create `.planning/phases/04-regression-net-and-check-gate/04-02-SUMMARY.md` when done. Include: the exact check command that ran, the recorded baseline NOTE/WARNING set, the current result, the delta (must be empty of new items), any source fixes made (globalVariables/DESCRIPTION), and — if the full check could not run — the exact environmental limitation and the fallback executed.
</output>