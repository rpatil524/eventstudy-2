---
phase: 04-regression-net-and-check-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test_contract_matrix.R
  - NEWS.md
autonomous: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "test_contract_matrix.R iterates every covered component × {strict, lenient} × ≥1 degenerate input and all assertions pass (TEST-02)"
    - "strict mode: each component raises an error naming the offending event_id/firm_symbol/component (TEST-02)"
    - "lenient mode: each model yields is_fitted=FALSE (or NA statistic for stats), NA downstream, no Inf/NaN leakage, one-warning invariant (TEST-02)"
    - "NEWS.md catalogs every milestone fix → the named test file + test_that label that locks it, with no fix left unmapped (TEST-01)"
  artifacts:
    - "tests/testthat/test_contract_matrix.R"
    - "NEWS.md (fix→test catalog section)"
  key_links:
    - "table-driven component registry (constructor + degenerate-data factory) drives both strict and lenient iterations"
    - "each catalog entry names a real, findable test_that label that currently passes"
---

<objective>
Build the durable regression net for the milestone: (1) a single table-driven `tests/testthat/test_contract_matrix.R` that exercises every covered component in both strict and lenient modes on ≥1 degenerate input and asserts the degenerate-input contract, and (2) a fix→test catalog in NEWS.md mapping every fix made this milestone to the named test that locks it, filling any gap where a fix lacks a dedicated named test.

Purpose: Lock Phases 1-3 behavior so the recurring audit-round bug churn stops (TEST-01, TEST-02).
Output: `tests/testthat/test_contract_matrix.R`, updated `NEWS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-net-and-check-gate/04-CONTEXT.md

# Contract helpers being exercised and the existing patterns to reuse
@R/contract.R
@tests/testthat/test_contract.R
@tests/testthat/helper-mock-data.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Table-driven contract-matrix skeleton — ONE component (MarketModel) × {strict, lenient} end-to-end</name>
  <files>tests/testthat/test_contract_matrix.R</files>
  <read_first>
    - R/contract.R — `.handle_degenerate()` builds the error/warning message as "component [event_id=..] [firm=..]: reason"; `.resolve_degenerate_mode()` resolves the mode. This is the contract each row asserts (per CONTRACT-03/04).
    - tests/testthat/test_contract.R lines 70-215 — the EXACT working pattern: construct model, set `m$degenerate_mode <- "strict"|"lenient"`, `m$event_id <- "EVT_K"`, `m$firm_symbol <- "FIRM_K"`, then `expect_error(m$fit(d), regexp="EVT_K")` (strict) or collect warnings via `withCallingHandlers` and assert `is_fitted==FALSE` + exactly one warning + all-NA abnormal_returns (lenient). Reuse this verbatim shape.
    - tests/testthat/helper-mock-data.R — degenerate factories already available and auto-loaded: `create_degenerate_model_data_insufficient(n_valid=1)`, `create_degenerate_model_data_zero_variance()`, `create_degenerate_volume_model_data_insufficient()`, `create_degenerate_volume_model_data_zero_variance()`, `create_degenerate_volatility_model_data_zero_var()`, `create_mock_factor_model_data()`.
  </read_first>
  <action>
    Create tests/testthat/test_contract_matrix.R with a table-driven registry as the FIRST-CLASS structure, seeded with exactly ONE component to prove the shape end-to-end before expanding in Task 2. Define a list named `contract_matrix_components` where each element is a named list of: `label` (human-readable component name used in test_that titles and error-regex assertions, e.g. "MarketModel"), `make` (a zero-arg closure returning a fresh model instance, e.g. `function() MarketModel$new()`), `data` (a zero-arg closure returning a degenerate estimation tibble, e.g. `function() create_degenerate_model_data_insufficient(n_valid = 1)`), and `kind` (one of "model" or "stat" — for this task only "model"). Seed the registry with a single MarketModel entry using `create_degenerate_model_data_insufficient`.

    Write a driver that iterates the registry and, for each `kind == "model"` entry, generates two `test_that` blocks:
      - strict: construct via `make()`, set `degenerate_mode <- "strict"`, `event_id <- "EVT_MX"`, `firm_symbol <- "FIRM_MX"`; assert `expect_error(fit(data()), regexp = "EVT_MX")`, and separately that the error message also contains `firm_symbol` and the component `label` (re-construct a fresh instance for each expect_error since fit() may mutate state — mirror test_contract.R's pattern of a fresh `m2`, `m3`).
      - lenient: construct via `make()`, set `degenerate_mode <- "lenient"`, identifiers as above; collect warnings with `withCallingHandlers(... invokeRestart("muffleWarning"))`; assert `is_fitted == FALSE`, exactly one warning during fit, then call `abnormal_returns(data())` collecting warnings and assert zero additional warnings, all `abnormal_returns` are NA, and none are Inf/NaN (`all(!is.infinite(x) & !is.nan(x))` over the non-NA subset — with all-NA that set is empty, which passes; the assertion guards the general case for later stat rows).

    Name test_that titles as "contract-matrix [strict] <label> — errors naming event_id/firm_symbol/component" and "contract-matrix [lenient] <label> — is_fitted FALSE, NA downstream, one warning, no Inf/NaN" so each row is individually findable for the catalog in Task 3. Keep the driver a plain `for` loop over the registry (no purrr needed) so failures report the offending `label`.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_contract_matrix.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error), sum(df$nb) >= 2)'</automated>
    <fails_when>The MarketModel strict row does not error naming EVT_MX/FIRM_MX/MarketModel, or the lenient row leaves is_fitted TRUE / emits ≠1 warning / leaves non-NA abnormal returns, or fewer than 2 assertions ran (registry not iterated).</fails_when>
  </verify>
  <done>test_contract_matrix.R exists with a table-driven registry seeded by MarketModel; strict and lenient rows both pass; the file runs green on its own.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the registry to every covered component (13 models + single/multi-event stats + missing-date pipeline path), both modes</name>
  <files>tests/testthat/test_contract_matrix.R</files>
  <read_first>
    - R/models.R and R/models_time_varying.R — constructor names to register: MarketModel, MarketAdjustedModel, ComparisonPeriodMeanAdjustedModel, CustomModel, FamaFrench3FactorModel, FamaFrench5FactorModel, Carhart4FactorModel, BHARModel, VolumeModel, VolatilityModel, RollingWindowModel, GARCHModel, DCCGARCHModel, LinearFactorModel. Factor models (FF3/FF5/Carhart4/LinearFactor) fit on the columns produced by `create_mock_factor_model_data()`; degenerate their estimation window by NA-ing all but one estimation row (reuse the `create_degenerate_model_data_insufficient` NA-out approach applied to a factor tibble via a small inline factory closure, or the zero-variance factory where the model divides by an index/factor sd).
    - R/single_event_test_statistics.R and R/multi_event_test_statistics.R — stat constructors: ARTTest, CARTTest, BHARTTest (single-event), CSectTTest, PatellZTest, BMPTest, KolariPynnonenTest, SignTest, GeneralizedSignTest, CalendarTimePortfolioTest (multi-event). For stats the "lenient degenerate" assertion is: the statistic is NA (not Inf/NaN) on degenerate input; there is no strict-error contract for every stat, so only assert the lenient NA-safety invariant for the "stat" kind (do NOT fabricate a strict error where the code does not raise one).
    - tests/testthat/test_multi_event_statistics.R and test_ar_car_test_statistics.R — reuse their existing degenerate-input construction (single-event-group, sigma==0 → NA) so the matrix stat rows mirror already-passing assertions rather than inventing new fixtures.
    - tests/testthat/test_prepare.R — the missing-event-date degradation path (PIPELINE-01) mode-honoring: strict errors, lenient warns + degrades. Register this as one "pipeline" kind row asserting both modes.
  </read_first>
  <action>
    Extend `contract_matrix_components` (do NOT restructure the driver — add rows) to cover every component listed in read_first. For each MODEL row provide `make` (constructor) and `data` (a degenerate factory closure appropriate to that model's guard: insufficient-obs for OLS/factor models, zero-variance for market/factor models, the volume/volatility-specific factories for VolumeModel/VolatilityModel). Where a model needs constructor arguments (e.g. CustomModel a supplied function, RollingWindowModel a window length), supply minimal valid arguments in the `make` closure so construction succeeds and the degenerate branch is what triggers.

    Add a `kind == "stat"` branch to the driver that generates ONE lenient-only test_that per stat row asserting the statistic column is NA and contains no Inf/NaN on the degenerate input (single-event-group or sigma==0), reusing the degenerate construction from the existing stat test files. Do NOT emit a strict row for stats unless the specific stat raises under strict mode — check the source before asserting.

    Add a `kind == "pipeline"` branch generating a strict row (`expect_error` on missing event date) and a lenient row (expect a warning + graceful degradation, no crash) for the PIPELINE-01 missing-date path, driven off `create_mock_task()` / `prepare_event_study()` as in test_prepare.R.

    Gate GARCHModel and DCCGARCHModel rows with `testthat::skip_if_not_installed("rugarch")` and `skip_if_not_installed("rmgarch")` respectively (per CONTEXT.md), so the matrix runs cleanly where those optional packages are absent. Keep every row's `label` distinct so Task 3 can cite exact test_that titles. Confirm each covered component from CONTRACT/MODELS/STATS/PIPELINE requirements has exactly one matrix row (models: 14 constructors incl. LinearFactor base; stats: 10; pipeline: 1).
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_contract_matrix.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error), sum(df$nb) >= 30)'</automated>
    <fails_when>Any component row fails its contract assertion, any row errors (uncaught), a GARCH/DCC row runs without rugarch/rmgarch installed instead of skipping, or fewer than ~30 assertions run (registry under-populated relative to the ~25 covered components × modes).</fails_when>
  </verify>
  <done>The registry covers all 14 model constructors, all 10 test statistics, and the missing-date pipeline path; every strict/lenient/pipeline row passes or (GARCH/DCC) skips cleanly; the matrix file is green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix→test catalog in NEWS.md — map every milestone fix to its named locking test, fill gaps</name>
  <files>NEWS.md</files>
  <read_first>
    - NEWS.md — current top section is "# EventStudy 0.40.0". Add a NEW top version section for this milestone (bump heading to the current DESCRIPTION Version, e.g. "# EventStudy 0.50.0") ABOVE the existing content; do not delete the 0.40.0 history.
    - The milestone fix set (git log 63d67a1..HEAD): CONTRACT-01..05 (test_contract.R), the review-fix rounds CR-01/CR-02, WR-01..05, IN-01..03, the GH-#7-class join fix (test_multi_event_statistics.R STATS-02), per-model migrations (per-model contract05_*.rds baselines in tests/testthat/fixtures/ + test_models.R), STATS-01/03/04 (test_ar_car_test_statistics.R, test_multi_event_statistics.R), PIPELINE-01/02/03 (test_prepare.R, test_export.R, test_cross_sectional.R), EXTERNAL-01..04 (test_panel.R, test_synthetic_control.R, test_models_time_varying.R). The new contract matrix (test_contract_matrix.R) is itself a catalog entry for TEST-02.
    - Run `git log --oneline 63d67a1..HEAD` to enumerate the exact fix commits; each fix/feat/CR/WR/IN commit is a catalog candidate.
  </read_first>
  <action>
    Add a new top version section in NEWS.md titled with the current package version (matching DESCRIPTION). Under it add a subsection "## Robustness Hardening — Regression Catalog" that lists every milestone fix mapped to the test that locks it. For each entry, name BOTH the test file AND a findable identifier: either a specific `test_that(...)` label or the contract-matrix row label from test_contract_matrix.R. Group by area (Contract, Models, Stats, Pipeline, External, Review fixes) mirroring the requirement families.

    Before writing, cross-check coverage by iterating the git-log fix commits: for each fix, confirm a named test exists (in an existing test_*.R OR as a contract-matrix row). If a fix has NO dedicated named test, add a targeted `test_that` to the most relevant existing test file (NOT a new file) that fails-before/passes-after that specific fix, then cite it in the catalog — this closes the TEST-01 "no fix without a test" gap. Do not invent behavioral changes; only add locking assertions for already-shipped fixes.

    Keep entries terse and greppable: "<fix, e.g. GH #7 event_id join> — locked by `tests/testthat/test_multi_event_statistics.R` :: `test_that(\"...event_id join...\")`". End with a one-line pointer that `test_contract_matrix.R` provides the cross-component degenerate-input regression net (TEST-02).
  </action>
  <verify>
    <automated>Rscript -e 'nz <- readLines("NEWS.md"); stopifnot(any(grepl("Regression Catalog", nz)), any(grepl("test_contract_matrix.R", nz, fixed=TRUE)), any(grepl("test_multi_event_statistics.R", nz, fixed=TRUE)), sum(grepl("test_[a-z_]+\\.R", nz)) >= 6); df <- as.data.frame(testthat::test_dir("tests/testthat", reporter="silent")); stopifnot(sum(df$failed)==0)'</automated>
    <fails_when>NEWS.md lacks the "Regression Catalog" heading, cites fewer than 6 distinct test files, omits the contract-matrix pointer, or a newly added gap-filling test_that fails / breaks the full suite.</fails_when>
  </verify>
  <done>NEWS.md has a new top version section with a fix→test catalog covering every milestone fix (each citing a real, findable test file + label); any gap is closed with a targeted test in an existing file; the full suite stays green.</done>
</task>

</tasks>

<threat_model>
No applicable threats. This is an internal R package with no untrusted-input boundary — the phase adds test files and documentation only, introduces no new network, filesystem-write, or deserialization surface, and no new package logic. Per CONTEXT.md constraints, "no applicable threats" is declared.
</threat_model>

<verification>
- `Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_contract_matrix.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'` — matrix green in isolation.
- `Rscript -e 'df <- as.data.frame(testthat::test_dir("tests/testthat", reporter="silent")); stopifnot(sum(df$failed)==0)'` — full suite (~1291 tests) stays green (TEST-03 sanity, fully gated in Plan 02).
- NEWS.md catalog cites ≥6 distinct test files and the contract-matrix pointer.
</verification>

<success_criteria>
- [ ] test_contract_matrix.R is table-driven and covers every covered component × {strict, lenient} × ≥1 degenerate input, all assertions pass (TEST-02).
- [ ] GARCH/DCC rows skip cleanly when rugarch/rmgarch absent.
- [ ] NEWS.md maps every milestone fix to a named locking test; no fix left unmapped (TEST-01).
- [ ] Full suite remains green.
</success_criteria>

<output>
Create `.planning/phases/04-regression-net-and-check-gate/04-01-SUMMARY.md` when done. Include: the final component-registry contents (count of model/stat/pipeline rows), any gap-filling test_that added and where, and the NEWS.md catalog structure.
</output>