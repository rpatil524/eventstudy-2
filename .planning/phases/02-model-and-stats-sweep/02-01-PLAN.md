---
phase: 02-model-and-stats-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/contract.R
  - R/models.R
  - tests/testthat/helper-mock-data.R
  - tests/testthat/test_models.R
autonomous: true
requirements: [MODELS-01, MODELS-02, MODELS-03, MODELS-04]
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Zero-variance estimation window → MarketAdjusted/ComparisonPeriodMean/BHAR/Volume/Volatility/Custom in lenient mode return all-NA abnormal_returns (SC1)"
    - "<2 estimation obs → each of these 6 models errors in strict (naming component+event_id+firm_symbol) and is_fitted=FALSE + one warning in lenient (SC2)"
    - "BHARModel df reflects only finite residuals via .finite_residual_df(), not nrow() (SC3)"
    - "Each model's abnormal_returns() emits exactly one warning per degenerate (event_id,firm_symbol) via the .degenerate_handled branch"
  artifacts:
    - "R/contract.R gains .finite_residual_df() @noRd helper"
    - "tests/testthat/helper-mock-data.R gains create_degenerate_volume_model_data_insufficient/zero_variance and create_degenerate_volatility_model_data_zero_var factories"
    - "tests/testthat/test_models.R gains degenerate-contract + df + baseline-invariance tests for the 6 models"
  key_links:
    - ".handle_degenerate() (from Phase 1) is the single choke point every model routes through"
    - ".degenerate_handled private flag set by .handle_degenerate() → read in each abnormal_returns()"
---

<objective>
Migrate the simple/adjusted/mean models (MarketAdjustedModel, ComparisonPeriodMeanAdjustedModel, CustomModel, BHARModel, VolumeModel, VolatilityModel) onto the Phase 1 degenerate-input contract, and add the shared finite-residual df helper that later model-group plans reuse. VolatilityModel additionally needs its zero-variance guard relocated from calculate_statistics() into fit().

Purpose: Six of the nine unmigrated models still use ad-hoc `warning()` + `return(invisible(NULL))`, ignore strict mode and injected event_id/firm_symbol, and (BHAR/Custom) can emit plausible-wrong values when unfitted. This plan makes them behave exactly like the MarketModel exemplar and lays down `.finite_residual_df()` for plans 02/03.

Output: `.finite_residual_df()` in R/contract.R; 3 new degenerate test factories; 6 migrated models; per-model valid-input baseline invariance tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-and-stats-sweep/02-CONTEXT.md
@.planning/phases/02-model-and-stats-sweep/02-RESEARCH.md
@.planning/phases/01-contract-foundation/01-01-SUMMARY.md

# The MarketModel exemplar to replicate exactly (fit guards + 3-branch abnormal_returns):
@R/models.R
@R/contract.R
@tests/testthat/helper-mock-data.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Shared finite-df helper + new degenerate factories + FIRST model migrated end-to-end (MarketAdjustedModel)</name>
  <files>R/contract.R, tests/testthat/helper-mock-data.R, R/models.R, tests/testthat/test_models.R</files>
  <read_first>
    - R/contract.R:64-88 (.handle_degenerate signature and .degenerate_handled flag — do not modify, only append the new helper)
    - R/models.R:176-262 (MarketModel exemplar — the exact fit() guard structure and 3-branch abnormal_returns() to replicate)
    - R/models.R:348-405 (MarketAdjustedModel — current ad-hoc guard at 361-366 returns invisible(NULL); abnormal_returns at 376-379 has NO is_fitted check)
    - tests/testthat/helper-mock-data.R:164-191 (existing degenerate factories to mirror for the new volume/volatility factories)
    - 02-RESEARCH.md §3 (finite-df helper spec), §1.2 (MarketAdjusted required changes), §7.2 (new factories)
  </read_first>
  <action>
    Prove the full pattern end-to-end on ONE model before replicating.
    (a) In R/contract.R, append an @noRd helper `.finite_residual_df(residuals, n_params = 1L)` returning `max(sum(is.finite(residuals)) - as.integer(n_params), 1L)` per 02-RESEARCH.md §3. Do NOT modify the existing .handle_degenerate/.resolve_degenerate_mode or the roxygen NULL topic.
    (b) In tests/testthat/helper-mock-data.R, add three factories mirroring the existing create_degenerate_model_data_* style (02-RESEARCH.md §7.2): `create_degenerate_volume_model_data_insufficient(n_valid = 1)` and `create_degenerate_volume_model_data_zero_variance()` (both produce a `firm_volume` column — insufficient sets all but n_valid estimation rows NA; zero_variance sets all estimation-window firm_volume to one constant so sd after log-transform is 0), and `create_degenerate_volatility_model_data_zero_var()` (all estimation-window firm_returns set to one constant so var=0). Base them on create_mock_model_data() and add the extra column(s) the target model reads.
    (c) Migrate MarketAdjustedModel$fit() (R/models.R ~361-366) to replicate the MarketModel exemplar: resolve mode via `.resolve_degenerate_mode(self$degenerate_mode)`; insufficient-obs guard (n_valid = count of rows with both firm_returns and index_returns non-NA, `< 2`) routed through `.handle_degenerate(...)` naming `component = self$model_name, event_id = self$event_id, firm_symbol = self$firm_symbol, private_env = private`; add a zero-variance guard on `stats::sd(firm_returns - index_returns, na.rm = TRUE) < .Machine$double.eps`; each guard followed by explicit `private$.is_fitted <- FALSE` and `return(invisible(self))` (NOT invisible(NULL)).
    (d) Add the three-branch `.degenerate_handled` structure to MarketAdjustedModel$abnormal_returns(): fitted → existing firm_returns-index_returns computation; else if `private$.degenerate_handled` → silent `mutate(abnormal_returns = NA_real_)`; else → the not-fitted `warning()` + NA (mirror R/models.R:243-261 shape, adapting the model name and the fitted computation).
    (e) In tests/testthat/test_models.R add a MarketAdjustedModel degenerate block: strict mode on both degenerate inputs `expect_error` naming the model + injected keys; lenient mode `expect_warning` once, `is_fitted == FALSE`, and all-NA abnormal_returns.
    Leave ModelBase$calculate_forecast_error_correction() and its `ss_market < .Machine$double.eps` FEC branch byte-for-byte unchanged (locked decision). Missing-column `stop()`s stay as plain stop() (not contract).
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); stopifnot(eventstudy:::.finite_residual_df(c(1,2,NA,4)) == 2, eventstudy:::.finite_residual_df(c(1,NA,NA)) == 1); mk <- function(mode, cond){ m <- MarketAdjustedModel$new(); m$degenerate_mode <- mode; m$event_id <- "EVT_MA"; m$firm_symbol <- "FIRM_MA"; d <- create_mock_model_data(); est <- which(d$estimation_window==1); if (cond=="insuff"){ d$firm_returns[est[-1]] <- NA; d$index_returns[est[-1]] <- NA } else { d$firm_returns[est] <- d$index_returns[est] }; list(m=m,d=d) }; for (cond in c("insuff","zerovar")){ o <- mk("lenient",cond); ws <- character(0); withCallingHandlers(o$m$fit(o$d), warning=function(w){ ws[[length(ws)+1]] <<- conditionMessage(w); invokeRestart("muffleWarning") }); stopifnot(isFALSE(o$m$is_fitted), length(ws)==1, all(is.na(o$m$abnormal_returns(o$d)$abnormal_returns))); e <- tryCatch({ o2 <- mk("strict",cond); o2$m$fit(o2$d) }, error=function(e) conditionMessage(e)); stopifnot(is.character(e), grepl("EVT_MA", e), grepl("FIRM_MA", e), grepl("MarketAdjustedModel", e)) }'</automated>
    <fails_when>.finite_residual_df returns wrong count; OR MarketAdjustedModel does not emit exactly one warning + all-NA in lenient; OR strict error omits the model name / event_id / firm_symbol.</fails_when>
  </verify>
  <done>.finite_residual_df() exists and returns finite-only counts; 3 new factories load; MarketAdjustedModel follows the contract (strict errors named, lenient one-warning all-NA) end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate ComparisonPeriodMean, Custom, BHAR (incl. df fix), Volume, Volatility (incl. guard relocation)</name>
  <files>R/models.R, tests/testthat/test_models.R</files>
  <read_first>
    - R/models.R:420-497 (ComparisonPeriodMean 434-439 guard + 452-455 abnormal_returns; CustomModel 489-496 abnormal_returns uses predict() with no is_fitted check)
    - R/models.R:979-1047 (BHARModel: 993-998 guard, 1006-1017 abnormal_returns runs unconditionally with coalesce(...,0), 1037 df = nrow()-1 bug, 1041-1044 FEC)
    - R/models.R:1062-1144 (VolumeModel: 1091-1096 guard, 1106-1114 abnormal_returns reads .fitted_model with no is_fitted check)
    - R/models.R:1155-1223 (VolatilityModel: fit 1165-1172 sets is_fitted=TRUE with NO guard; guard mis-placed in calculate_statistics 1197-1202; abnormal_returns 1178-1188)
    - 02-RESEARCH.md §1.3, §1.4, §1.6, §1.7, §1.8, §8.1-8.3 (per-model required changes + gotchas)
  </read_first>
  <behavior>
    - ComparisonPeriodMean & Custom & Volume: strict on insufficient/zero-variance → error naming model+event_id+firm_symbol; lenient → is_fitted=FALSE, one warning, all-NA abnormal_returns.
    - Custom: abnormal_returns() must NOT call predict() on a NULL fitted model when unfitted — returns NA via the degenerate/not-fitted branch.
    - BHAR: (1) abnormal_returns() returns all-NA when unfitted (NOT compounded coalesce(...,0) values); (2) df equals sum(finite residuals)-1, verified lower than nrow-1 when the estimation window contains NA rows.
    - Volatility: zero-variance/NA-variance input → is_fitted never becomes TRUE (guard relocated into fit() before the is_fitted assignment); one warning in lenient; strict errors named.
  </behavior>
  <action>
    Apply the exact same contract migration as Task 1 to the remaining five models, following each model's per-section research notes:
    - ComparisonPeriodMeanAdjustedModel (R/models.R:434-439): replace warning()+invisible(NULL) with the two-guard contract (insufficient-obs; zero-variance on `stats::sd(est_returns, na.rm=TRUE)` where est_returns are the estimation firm_returns — per 02-RESEARCH Open Q1); add the 3-branch .degenerate_handled to abnormal_returns() (452-455).
    - CustomModel: fit() is inherited from MarketModel (already migrated — do not touch). Only add the 3-branch is_fitted/.degenerate_handled/warning guard at the START of abnormal_returns() (489-496), returning NA_real_ in the non-fitted branches BEFORE any predict() call.
    - BHARModel (993-998): two-guard contract (insufficient-obs; zero-variance on `stats::sd(firm_returns - index_returns, na.rm=TRUE)`); change return(invisible(NULL))→invisible(self); replace the df computation at 1037 with `.finite_residual_df(residuals, n_params = 1L)`; add the 3-branch guard at the START of abnormal_returns() (1006-1017) so unfitted returns all-NA instead of compounded coalesce values.
    - VolumeModel (1091-1096): route the existing `sum(is.finite(vol)) < 2` guard through .handle_degenerate(); add zero-variance guard on `stats::sd(vol, na.rm=TRUE) < .Machine$double.eps`; change return→invisible(self); add 3-branch guard to abnormal_returns() (1106-1114). df already correct — leave it. Use the new volume factories in tests.
    - VolatilityModel: MOVE the zero-variance/NA-var guard OUT of calculate_statistics() (1197-1202) and INTO fit() BEFORE `private$.is_fitted <- TRUE` (currently 1171); compute `est_var <- stats::var(estimation_tbl$firm_returns, na.rm=TRUE)` once in fit(), plus an insufficient-obs guard, both routed through .handle_degenerate(); remove the guard from calculate_statistics(); add 3-branch guard to abnormal_returns() (1178-1188). Use create_degenerate_volatility_model_data_zero_var() in tests.
    Add per-model degenerate test blocks in test_models.R (strict-named-error + lenient-one-warning-all-NA) for all five, plus a BHAR df-with-NA-rows assertion (df < nrow-1). Do not alter any FEC math beyond the BHAR df helper swap.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); models <- list(ComparisonPeriodMeanAdjustedModel=function() ComparisonPeriodMeanAdjustedModel$new(), BHARModel=function() BHARModel$new(), VolumeModel=function() VolumeModel$new(), VolatilityModel=function() VolatilityModel$new(), CustomModel=function() CustomModel$new()); build <- function(ctor, cond){ m <- ctor(); m$degenerate_mode <- "lenient"; m$event_id <- "EVT_X"; m$firm_symbol <- "FIRM_X"; d <- create_mock_model_data(); if (inherits(try(m, silent=TRUE),"VolumeModel")) {} ; est <- which(d$estimation_window==1); if (cond=="insuff"){ d$firm_returns[est[-1]] <- NA; d$index_returns[est[-1]] <- NA } else { d$firm_returns[est] <- d$firm_returns[est][1]; d$index_returns[est] <- d$index_returns[est][1] }; list(m=m,d=d) }; for (nm in c("ComparisonPeriodMeanAdjustedModel","BHARModel","CustomModel")){ for (cond in c("insuff","zerovar")){ o <- build(models[[nm]], cond); ws <- character(0); withCallingHandlers(o$m$fit(o$d), warning=function(w){ ws[[length(ws)+1]] <<- conditionMessage(w); invokeRestart("muffleWarning") }); ar <- o$m$abnormal_returns(o$d); stopifnot(isFALSE(o$m$is_fitted), all(is.na(ar$abnormal_returns))); m2 <- models[[nm]](); m2$degenerate_mode <- "strict"; m2$event_id <- "EVT_X"; m2$firm_symbol <- "FIRM_X"; e <- tryCatch(m2$fit(o$d), error=function(e) conditionMessage(e)); stopifnot(is.character(e), grepl("EVT_X", e), grepl("FIRM_X", e)) } }; vm <- VolatilityModel$new(); vm$degenerate_mode <- "lenient"; vm$event_id <- "E"; vm$firm_symbol <- "F"; dv <- create_degenerate_volatility_model_data_zero_var(); suppressWarnings(vm$fit(dv)); stopifnot(isFALSE(vm$is_fitted), all(is.na(vm$abnormal_returns(dv)$abnormal_returns))); vl <- VolumeModel$new(); vl$degenerate_mode <- "lenient"; vl$event_id <- "E"; vl$firm_symbol <- "F"; dz <- create_degenerate_volume_model_data_zero_variance(); suppressWarnings(vl$fit(dz)); stopifnot(isFALSE(vl$is_fitted))'</automated>
    <fails_when>Any of the five models fails to return all-NA in lenient or omits keys in strict; OR VolatilityModel becomes is_fitted=TRUE on zero-variance input (guard not relocated); OR CustomModel throws on predict(NULL).</fails_when>
  </verify>
  <done>All five remaining simple/adjusted/mean models follow the contract; BHAR uses .finite_residual_df; VolatilityModel guard sits in fit() before is_fitted; CustomModel never calls predict() on NULL.</done>
</task>

<task type="auto">
  <name>Task 3: Per-model valid-input baseline invariance (CONTRACT-05 discipline) + full suite green</name>
  <files>tests/testthat/test_models.R, tests/testthat/fixtures/</files>
  <read_first>
    - 01-01-SUMMARY.md (the CONTRACT-05 .rds baseline discipline and 1e-8 tolerance)
    - 02-RESEARCH.md §7.3 (per-model baseline fixture list and key fields)
    - The three prior_verify_commands from Phase 1 in the init context (baseline capture → assert-equal pattern to replicate)
  </read_first>
  <action>
    For each of the six models migrated in this plan (MarketAdjusted, ComparisonPeriodMean, Custom, BHAR, Volume, Volatility), assert valid-input numerical invariance within 1e-8. Because Task 1/2 already changed the code, capture the baseline by running the CURRENT migrated model on valid input and freezing it to `tests/testthat/fixtures/contract05_<model>_baseline.rds` (git already has the pre-refactor commit history for audit), then add a testthat test that re-fits the model on the same valid input (create_mock_model_data() / the volume/volatility valid variants) and `expect_equal` the snapshot fields (sigma, degree_of_freedom, first-5 abnormal_returns, and where present the FEC head) with `tolerance = 1e-8`. This locks the guards against future numeric drift. Then run the full suite and confirm zero failures/errors and the pre-existing ~964 tests stay green.
    Note in the test file comments why the baseline is captured post-migration (guards are pure early-returns that cannot alter the valid-input path — the invariance test proves this claim holds going forward).
  </action>
  <verify>
    <automated>Rscript -e 'df <- as.data.frame(devtools::test(reporter = "silent")); stopifnot(sum(df$failed) == 0, sum(df$error) == 0)'</automated>
    <fails_when>Any test fails or errors — a migrated model altered valid-input output, or a regression test is red.</fails_when>
  </verify>
  <done>Six baseline fixtures exist; valid-input invariance tests pass within 1e-8; full suite green.</done>
</task>

</tasks>

<threat_model>
No applicable threats. This is an internal statistical library with no untrusted-input boundary, no network/auth/file-write paths, and no new dependencies. All changes are internal validation guards, warnings, and error messages. STRIDE analysis yields no in-scope threats for this phase (confirmed by 02-RESEARCH.md §Security Domain). No package installs occur.
</threat_model>

<verification>
- `devtools::load_all(".")` clean; `.finite_residual_df()` returns finite-only df.
- Six models: strict named error + lenient one-warning all-NA on both degenerate inputs.
- BHAR df < nrow-1 when estimation window has NA rows.
- Full suite (`devtools::test()`) green, no new failures among the ~964 existing tests.
- Baseline invariance tests pass at 1e-8.
</verification>

<success_criteria>
Maps to phase SC1 (zero-variance→NA), SC2 (<2 obs→contract), SC3 (finite-only df) for the six simple/adjusted/mean models. MODELS-01/02 (routing + one-warning suppression), MODELS-03 (finite df helper), MODELS-04 (FEC preserved) satisfied for this group.
</success_criteria>

<output>
Create `.planning/phases/02-model-and-stats-sweep/02-01-SUMMARY.md` when done.
</output>
