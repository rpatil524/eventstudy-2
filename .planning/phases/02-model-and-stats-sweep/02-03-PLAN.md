---
phase: 02-model-and-stats-sweep
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - R/models.R
  - R/models_time_varying.R
  - tests/testthat/test_models_time_varying.R
autonomous: true
requirements: [MODELS-01, MODELS-02, MODELS-03, MODELS-04]
estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "RollingWindow, GARCH, DCC-GARCH route insufficient-obs and zero-variance PRE-CALL conditions through .handle_degenerate() (SC1, SC2)"
    - "GARCH/DCC guards fire BEFORE the first rugarch/rmgarch call; the existing failure tryCatch/convergence warnings are left byte-for-byte untouched (Phase 3 boundary)"
    - "Each of the three models' abnormal_returns() carries the .degenerate_handled branch → one warning per degenerate event"
    - "GARCH FEC uses n_valid (finite obs) instead of nrow(estimation_tbl)"
  artifacts:
    - "tests/testthat/test_models_time_varying.R gains degenerate-contract tests (rugarch/rmgarch guarded by skip_if_not_installed)"
    - "Conditional (skip_if_not_installed) valid-input baseline checks for GARCH/DCC; unconditional for RollingWindow"
  key_links:
    - "Pre-call guards insert BEFORE rugarch::ugarchspec() (R/models.R:865) and BEFORE returns_mat cbind (R/models_time_varying.R:214)"
    - "RollingWindow's three plain warning() sites all become .handle_degenerate() calls"
---

<objective>
Migrate the time-varying / external-package-bound models (RollingWindowModel, GARCHModel, DCCGARCHModel) onto the degenerate-input contract by inserting PRE-CALL guards only. The rugarch/rmgarch FAILURE wrapping (tryCatch, convergence, non-finite covariance) is explicitly Phase 3 and must be left untouched.

Purpose: These models need the contract's degenerate-input guards before the external calls, but the external-failure handling is out of scope. RollingWindow is pure in-package R (all guards migrate). GARCH/DCC insert guards ahead of the external call and leave the safely()/convergence paths alone.

Output: three migrated models with pre-call guards + .degenerate_handled branches; skip-guarded baseline/contract tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-and-stats-sweep/02-CONTEXT.md
@.planning/phases/02-model-and-stats-sweep/02-RESEARCH.md

# The migrated MarketModel exemplar + contract helpers + degenerate factories:
@R/models.R
@R/models_time_varying.R
@R/contract.R
@tests/testthat/helper-mock-data.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Migrate RollingWindowModel (pure in-package, all three guards) end-to-end</name>
  <files>R/models_time_varying.R, tests/testthat/test_models_time_varying.R</files>
  <read_first>
    - R/models_time_varying.R:1-165 (RollingWindow full class)
    - R/models_time_varying.R:37-42 (Guard 1: insufficient est obs, uses nrow not finite count), :45-50 (Guard 2: effective window < 3), :91-96 (Guard 3: last-window NA params) — all three are plain warning()+invisible(self)
    - R/models_time_varying.R:105-120 (abnormal_returns — two-branch, missing .degenerate_handled)
    - R/models_time_varying.R:141 (df = ws-2 — KEEP AS-IS, rolling-domain df), :152-157 (FEC — KEEP)
    - R/models.R:176-262 (MarketModel exemplar for the .handle_degenerate call shape + 3-branch abnormal_returns)
    - 02-RESEARCH.md §1.9 (RollingWindow required changes + the note that the whole model is in-package → all three guards migrate in Phase 2)
  </read_first>
  <action>
    Migrate all three RollingWindowModel guards to the contract (RollingWindow is entirely in-package R — no external-call boundary, so no Phase 3 carve-out applies here):
    - Resolve mode once at the top of fit() via .resolve_degenerate_mode(self$degenerate_mode).
    - Guard 1 (37-42): additionally compute a finite-obs count `n_valid <- sum(!is.na(estimation_tbl$firm_returns) & !is.na(estimation_tbl$index_returns))` and trigger the degenerate path when `n_valid < self$min_obs` (not just nrow); route through .handle_degenerate() with a condition naming the shortfall; keep private$.is_fitted <- FALSE + return(invisible(self)).
    - Guard 2 (45-50, effective window < 3) and Guard 3 (91-96, last-window NA params): replace the plain warning() with .handle_degenerate() (same component/event_id/firm_symbol/private_env args), keeping the explicit is_fitted<-FALSE + return.
    - Add the 3-branch .degenerate_handled structure to abnormal_returns() (105-120).
    - Leave df = ws-2 (141) and the FEC (152-157) unchanged (rolling-domain df is correct per 02-RESEARCH §3).
    Add a RollingWindowModel degenerate block to test_models_time_varying.R: strict named error + lenient one-warning all-NA on insufficient obs (and on a zero-variance estimation window that drives Guard 3 to NA params). RollingWindow needs no skip_if_not_installed.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); mk <- function(mode){ m <- RollingWindowModel$new(); m$degenerate_mode <- mode; m$event_id <- "EVT_RW"; m$firm_symbol <- "FIRM_RW"; d <- create_mock_model_data(); est <- which(d$estimation_window==1); d$firm_returns[est[-1]] <- NA; d$index_returns[est[-1]] <- NA; list(m=m,d=d) }; o <- mk("lenient"); ws <- character(0); withCallingHandlers(o$m$fit(o$d), warning=function(w){ ws[[length(ws)+1]] <<- conditionMessage(w); invokeRestart("muffleWarning") }); stopifnot(isFALSE(o$m$is_fitted), length(ws)==1, all(is.na(o$m$abnormal_returns(o$d)$abnormal_returns))); m2 <- mk("strict")$m; e <- tryCatch(m2$fit(mk("strict")$d), error=function(e) conditionMessage(e)); stopifnot(is.character(e), grepl("EVT_RW", e), grepl("FIRM_RW", e))'</automated>
    <fails_when>RollingWindow emits more than one warning / non-NA on insufficient obs, or the strict path does not error with the injected keys.</fails_when>
  </verify>
  <done>All three RollingWindow guards route through .handle_degenerate(); abnormal_returns() has the 3-branch structure; df/FEC unchanged; contract holds end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Insert PRE-CALL guards for GARCHModel + DCCGARCHModel (leave failure wrapping untouched) + FEC n_valid fix + skip-guarded tests + full suite green</name>
  <files>R/models.R, R/models_time_varying.R, tests/testthat/test_models_time_varying.R, tests/testthat/fixtures/</files>
  <read_first>
    - R/models.R:844-965 (GARCHModel: requireNamespace 857-860 KEEP; ugarchspec at ~865 = insertion point; safely(ugarchfit) 878 + convergence 887-896 + warnings 899/903 = Phase 3 LEAVE UNTOUCHED; abnormal_returns 910-927; df 947 KEEP; FEC 957-962 uses nrow → change to n_valid)
    - R/models_time_varying.R:177-347 (DCCGARCHModel: requireNamespace 202-209 KEEP; returns_mat cbind at 214 = insertion point; safely(dccfit) 236 + rcov check 244-247 + warnings 253/259 = Phase 3 LEAVE UNTOUCHED; abnormal_returns 266-280; df 324 KEEP; FEC 337-344)
    - 02-RESEARCH.md §1.10, §1.11, §6, §8.5, §8.7, Pitfall 4 (exact insertion points + the strict Phase 2/3 boundary)
  </read_first>
  <action>
    GARCHModel — insert BEFORE rugarch::ugarchspec() (R/models.R:865), AFTER the existing requireNamespace check (leave 857-860 as-is): resolve mode; insufficient-obs guard on `n_valid <- sum(!is.na(estimation_tbl$firm_returns) & !is.na(estimation_tbl$index_returns))` (`< 2`); zero-variance guard on `stats::sd(estimation_tbl$index_returns, na.rm=TRUE) < .Machine$double.eps` (index is the external regressor in the mean equation) — both routed through .handle_degenerate() with explicit private$.is_fitted <- FALSE + return(invisible(self)). Change the FEC call's estimation_window_length from `nrow(estimation_tbl)` to `n_valid` (957-962). Add the .degenerate_handled branch to abnormal_returns() (910-927). Do NOT touch the safely()/convergence/failure warnings (878, 887-904) or ugarchspec/ugarchfit calls — those are Phase 3.
    DCCGARCHModel — insert BEFORE `returns_mat <- cbind(...)` (R/models_time_varying.R:214), AFTER requireNamespace (leave 202-209): resolve mode; joint insufficient-obs guard on finite (firm_returns, index_returns) pairs; zero-variance guard on BOTH series independently — `sd(firm_returns) < eps` OR `sd(index_returns) < eps`, with the condition string naming which series triggered (02-RESEARCH §8.5) — routed through .handle_degenerate() with explicit is_fitted<-FALSE + return. Add the .degenerate_handled branch to abnormal_returns() (266-280). Do NOT touch dccfit()/rcov/failure warnings (236-260). df (324) KEEP.
    Tests (test_models_time_varying.R): wrap GARCH/DCC degenerate-contract tests in `skip_if_not_installed("rugarch")` / `skip_if_not_installed("rmgarch")`. Assert strict named error + lenient one-warning all-NA on insufficient obs and zero-variance (both series for DCC). Capture GARCH/DCC valid-input baselines as CODE inside the skip block (compute + save on first run, compare within 1e-8 after) rather than a checked-in .rds requiring the package (02-RESEARCH Open Q3). Then run the full suite green.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); rg <- requireNamespace("rugarch", quietly=TRUE); rm <- requireNamespace("rmgarch", quietly=TRUE); if (rg){ m <- GARCHModel$new(); m$degenerate_mode <- "lenient"; m$event_id <- "E"; m$firm_symbol <- "F"; d <- create_degenerate_model_data_insufficient(); ws <- character(0); withCallingHandlers(m$fit(d), warning=function(w){ ws[[length(ws)+1]] <<- conditionMessage(w); invokeRestart("muffleWarning") }); stopifnot(isFALSE(m$is_fitted), all(is.na(m$abnormal_returns(d)$abnormal_returns))); m2 <- GARCHModel$new(); m2$degenerate_mode <- "strict"; m2$event_id <- "E"; m2$firm_symbol <- "F"; e <- tryCatch(m2$fit(d), error=function(e) conditionMessage(e)); stopifnot(grepl("E", e), grepl("F", e)) } else message("SKIP rugarch"); if (rm){ dm <- DCCGARCHModel$new(); dm$degenerate_mode <- "lenient"; dm$event_id <- "E"; dm$firm_symbol <- "F"; dz <- create_degenerate_model_data_zero_variance(); suppressWarnings(dm$fit(dz)); stopifnot(isFALSE(dm$is_fitted), all(is.na(dm$abnormal_returns(dz)$abnormal_returns))) } else message("SKIP rmgarch"); df <- as.data.frame(devtools::test(reporter = "silent")); stopifnot(sum(df$failed) == 0, sum(df$error) == 0)'</automated>
    <fails_when>When rugarch/rmgarch are installed: GARCH/DCC do not follow the contract on degenerate input (guard not inserted before the external call), OR the full suite fails/errors. When absent: the model tests skip and the suite still passes.</fails_when>
  </verify>
  <done>GARCH + DCC pre-call guards fire before the external call; failure wrapping untouched; GARCH FEC uses n_valid; abnormal_returns() 3-branch on all three; skip-guarded baselines invariant at 1e-8; full suite green.</done>
</task>

</tasks>

<threat_model>
No applicable threats. Internal statistical library; the only external touchpoints (rugarch/rmgarch) are optional Suggests already guarded by requireNamespace() and their failure handling is out of scope (Phase 3). No untrusted input, no network/auth/file-write, no new dependencies, no package installs. STRIDE yields no in-scope threats (02-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- RollingWindow: all three guards via .handle_degenerate(); one-warning all-NA lenient; strict named error.
- GARCH/DCC: pre-call guards fire before rugarch/rmgarch; failure tryCatch/convergence warnings unchanged (grep-confirmable they still exist); FEC uses n_valid for GARCH.
- GARCH/DCC contract tests skip cleanly when packages absent; assert contract when present.
- Full suite green.
</verification>

<success_criteria>
Maps to phase SC1/SC2 for the time-varying/external family (pre-call guards only; MODELS-04 FEC preserved/fixed for GARCH). MODELS-01/02/03/04 satisfied for RollingWindow, GARCH, DCC-GARCH within the Phase 2/3 boundary.
</success_criteria>

<output>
Create `.planning/phases/02-model-and-stats-sweep/02-03-SUMMARY.md` when done.
</output>
