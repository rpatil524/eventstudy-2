---
phase: 02-model-and-stats-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - R/single_event_test_statistics.R
  - R/multi_event_test_statistics.R
  - tests/testthat/test_single_event_statistics.R
  - tests/testthat/test_multi_event_statistics.R
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04]
estimate:
  tokens: 75000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "sigma == 0 (or < .Machine$double.eps) → ARTTest/CARTTest/BHARTTest return NA rather than Inf/NaN (SC of STATS-01)"
    - "A firm appearing in >1 event does not inflate Patell/BMP/KP/CSect denominators or counts (SC4) — locked with regression tests"
    - "A firm dropping mid-window (NA gap) does not corrupt subsequent cumulative CAR/CAAR values (SC5) — locked with a regression test"
    - "n_events == 1 yields NA in PatellZTest aar_z/caar_z and SignTest sign_z instead of a finite-but-invalid number (STATS-04)"
  artifacts:
    - "R/single_event_test_statistics.R gains sigma-denominator guards in ARTTest, CARTTest, BHARTTest"
    - "R/multi_event_test_statistics.R gains n_events<=1 guards in PatellZTest and SignTest"
    - "test_single_event_statistics.R + test_multi_event_statistics.R gain STATS-01/02/03/04 regression tests"
  key_links:
    - "Multi-event event_id joins + coalesce cumsum chains are ALREADY correct → verify-and-lock, do NOT rewrite"
    - "Only surgical denominator guards are added on top of the existing (correct) join/cumsum machinery"
---

<objective>
Lock the test-statistic correctness that Phase-1 groundwork and prior commits already established (event_id joins for STATS-02, coalesce cumsum chains for STATS-03) with regression tests, and add the surgical denominator guards the research found: sigma==0 → NA in the single-event stats (STATS-01), and n_events==1 → NA in PatellZTest and SignTest (STATS-04).

Purpose: The many-to-many join fix and the NA-safe cumsum chains are already in the code — the risk is regression, not absence. This plan proves them with firm-in-2-events and firm-drops-mid-window tests, then closes the two remaining Inf-producing denominator gaps.

Output: sigma guards in 3 single-event stats; n_events guards in 2 multi-event stats; regression tests for all four STATS requirements. Touches ONLY the two stats files + their tests — no model files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-and-stats-sweep/02-CONTEXT.md
@.planning/phases/02-model-and-stats-sweep/02-RESEARCH.md

@R/single_event_test_statistics.R
@R/multi_event_test_statistics.R
@tests/testthat/helper-mock-data.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Single-event sigma==0 denominator guards (STATS-01) end-to-end</name>
  <files>R/single_event_test_statistics.R, tests/testthat/test_single_event_statistics.R</files>
  <read_first>
    - R/single_event_test_statistics.R:64-75 (ARTTest: ar_t = abnormal_returns / sigma — %||% only handles NULL, not zero)
    - R/single_event_test_statistics.R:105-125 (CARTTest: car_t = car / (sqrt(event_window_length) * sigma); the dist sigma already uses pmax(...) but car_t itself is unguarded)
    - R/single_event_test_statistics.R:167-193 (BHARTTest: bhar_se = sigma * sqrt(n); bhar_t = bhar / bhar_se — sigma=0 → Inf)
    - 02-RESEARCH.md §5.3 (exact guards; note cumsum(abnormal_returns) NA-cascade in CART is correct behavior, do not "fix")
  </read_first>
  <action>
    Add a near-zero sigma guard so each single-event t-statistic returns NA_real_ instead of Inf when the model produced sigma==0 (a degenerate-model artifact). Use the condition `is.na(sigma) || sigma < .Machine$double.eps` (BHAR guards on `bhar_se` equivalently):
    - ARTTest (72): wrap ar_t so it is NA_real_ when sigma is NA or near-zero, else abnormal_returns / sigma.
    - CARTTest (117): wrap car_t so it is NA_real_ when sigma is NA or near-zero, else car / (sqrt(event_window_length) * sigma). Leave the car_t_dist construction and its pmax() sigma untouched, and leave cumsum(abnormal_returns) as-is (NA cascade on degenerate model input is correct, per 02-RESEARCH §5.3).
    - BHARTTest (190): wrap bhar_t so it is NA_real_ when bhar_se is NA or near-zero, else bhar / bhar_se.
    Do NOT alter the existing sigma extraction (`%||% NA_real_`), the distributional constructions, or any valid-input arithmetic — the guard only intercepts the zero/near-zero denominator branch.
    Add a test in test_single_event_statistics.R: construct a fitted-model stub whose statistics$sigma == 0 (or a degenerate model producing sigma==0), run each of the three tests, and assert every t column is NA and finite-valued NON-degenerate inputs are unchanged (regression: also assert a normal fit still yields finite t-stats).
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); mk <- function(sig){ structure(list(statistics=list(sigma=sig, degree_of_freedom=100)), class="stub") }; assign("statistics", function(x) x$statistics, envir=globalenv()); model0 <- list(statistics=list(sigma=0, degree_of_freedom=100)); class(model0) <- "R6"; d <- create_mock_model_data(); d$firm_returns <- d$firm_returns; d2 <- within(d, {}); modelfake <- local({ e <- new.env(); e$statistics <- list(sigma=0, degree_of_freedom=100); structure(list(), statistics=e$statistics) }); m <- MarketModel$new(); dm <- create_degenerate_model_data_zero_variance(); suppressWarnings(m$fit(dm)); dd <- m$abnormal_returns(dm); art <- ARTTest$new()$compute(dd, m); cart <- CARTTest$new()$compute(dd, m); bhart <- BHARTTest$new()$compute(dd, m); stopifnot(all(is.na(art$ar_t)), all(is.na(cart$car_t)), all(is.na(bhart$bhar_t)), !any(is.infinite(c(art$ar_t, cart$car_t, bhart$bhar_t))))'</automated>
    <fails_when>Any single-event t column is Inf/NaN (rather than NA) when the model's sigma is 0.</fails_when>
  </verify>
  <done>ARTTest/CARTTest/BHARTTest return NA (never Inf/NaN) on sigma==0; valid-input t-stats remain finite and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-event n_events==1 guards (STATS-04) + STATS-02 join-correctness regression tests</name>
  <files>R/multi_event_test_statistics.R, tests/testthat/test_multi_event_statistics.R</files>
  <read_first>
    - R/multi_event_test_statistics.R:108-140 (PatellZTest: Q_total = sqrt(sum(Q_i)) at 140 → aar_z/caar_z finite-but-invalid when n_events==1)
    - R/multi_event_test_statistics.R:206-216 (SignTest: sign_z at 206-208 defined but invalid at n_valid_events==1)
    - R/multi_event_test_statistics.R:33-52 (CSectT guards — ALREADY correct: sd of single value → NA), :413-435 (BMP guard already correct), :570-631 (KP inherits NA from bmp_t)
    - 02-RESEARCH.md §5.1 (join map — all confirmed on event_id), §5.4 (exact STATS-04 gaps: only Patell + Sign need a guard)
  </read_first>
  <action>
    STATS-04 guards (only the two the research flagged; the others are already correct — do NOT touch CSectT/BMP/KP guards):
    - PatellZTest: add a guard so when the number of valid events is <= 1, aar_z and caar_z are NA_real_ instead of the statistically-invalid finite z (research §5.4 — e.g. `ifelse(n_valid_events <= 1, NA_real_, <existing z>)` or an early NA path in the summarise producing aar_z/caar_z).
    - SignTest: add a guard so when n_valid_events < 2, sign_z is NA_real_ (wrap the existing sign_z ifelse).
    STATS-02 regression tests (verify-and-lock; do NOT rewrite the joins — they are already on event_id per 02-RESEARCH §5.1): in test_multi_event_statistics.R, build a scenario where one firm_symbol appears in TWO distinct event_id rows and assert that Patell, BMP, KP, and CSect denominators/counts reflect the number of EVENTS (each event contributes once), NOT an inflated many-to-many product. Assert n_events / n_valid_events equals the true event count and the resulting statistics match the per-event-once computation (a firm in 2 events must not double-count).
    Add STATS-04 tests: an n_events==1 dataset yields NA for Patell aar_z/caar_z and Sign sign_z (and confirm KP stays NA via inherited bmp_t, CSect stays NA — already-correct baselines locked).
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_multi_event_statistics.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>Patell aar_z/caar_z or Sign sign_z returns a finite number for n_events==1; OR a firm-in-2-events scenario inflates any multi-event denominator/count; OR the join-correctness regression tests are red.</fails_when>
  </verify>
  <done>Patell + Sign return NA at n_events==1; firm-in-2-events regression tests prove no many-to-many inflation across Patell/BMP/KP/CSect; existing correct guards left untouched.</done>
</task>

<task type="auto">
  <name>Task 3: STATS-03 NA-safe CAR/CAAR mid-window-drop regression tests + full suite green</name>
  <files>tests/testthat/test_multi_event_statistics.R, tests/testthat/helper-mock-data.R</files>
  <read_first>
    - 02-RESEARCH.md §5.2 (the confirmed coalesce(x,0) cumsum locations across CSect/Sign/BMP/KP/Patell — verify-and-lock, no rewrite)
    - R/multi_event_test_statistics.R:44,50 (CSect per-event CAR + CAAR cumsum(coalesce(...))), :157 (Patell CSAR), :209,217 (Sign)
  </read_first>
  <action>
    Add a firm-drops-mid-window regression test (STATS-03): construct a multi-event dataset (optionally via a small helper in helper-mock-data.R) where one firm's abnormal_returns are NA for a contiguous block in the MIDDLE of the event window (the firm "drops out"), and assert that the cumulative CAR/CAAR values AFTER the gap are computed as if the NA contributed 0 to the running sum (i.e. cumsum(coalesce(ar,0)) continues correctly rather than turning every subsequent CAR into NA). Assert the pre-gap CARs, the gap, and the post-gap CARs match the coalesce-based expectation for at least CSectTTest (and spot-check one of Sign/BMP). This locks the existing coalesce chains against future regression — the code is already correct (02-RESEARCH §5.2), the test proves it and guards it.
    Then run the full suite and confirm zero failures/errors and the ~964 existing tests stay green.
  </action>
  <verify>
    <automated>Rscript -e 'df <- as.data.frame(devtools::test(reporter = "silent")); stopifnot(sum(df$failed) == 0, sum(df$error) == 0)'</automated>
    <fails_when>A mid-window NA gap corrupts post-gap cumulative CAR/CAAR (they become NA or shift), OR any suite test fails/errors.</fails_when>
  </verify>
  <done>Mid-window-drop regression test passes proving NA gaps do not corrupt subsequent CARs; full suite green.</done>
</task>

</tasks>

<threat_model>
No applicable threats. Internal statistical library, no untrusted-input boundary, no network/auth/file-write/dependency changes, no package installs. All changes are denominator guards and regression tests. STRIDE yields no in-scope threats (02-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- ARTTest/CARTTest/BHARTTest → NA (never Inf) on sigma==0.
- Patell aar_z/caar_z and Sign sign_z → NA at n_events==1.
- Firm-in-2-events: no many-to-many inflation across Patell/BMP/KP/CSect (regression tests).
- Firm-drops-mid-window: post-gap CARs correct (regression test).
- Full suite green.
</verification>

<success_criteria>
Maps to phase SC4 (multi-event no inflation) and SC5 (NA-safe CAR chain), plus STATS-01 (sigma guards) and STATS-04 (n_events==1 → NA). STATS-01/02/03/04 all satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-model-and-stats-sweep/02-04-SUMMARY.md` when done.
</output>
