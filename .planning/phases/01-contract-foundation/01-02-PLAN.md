---
phase: 01-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/testthat/helper-mock-data.R
  - tests/testthat/test_contract.R
  - tests/testthat/test_parameter_set.R
  - NAMESPACE
  - man/degenerate-input-contract.Rd
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-03, CONTRACT-04, CONTRACT-05]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "?degenerate-input-contract Rd file is generated by roxygen2 and installs cleanly (CONTRACT-01 findable)"
    - "test_contract.R proves strict-mode error names event_id/firm_symbol, lenient-mode is_fitted FALSE + NA downstream + exactly one warning, and mode switching via field and via option both work without reload (CONTRACT-02/03/04)"
    - "A CONTRACT-05 regression asserts valid-input MarketModel statistics equal a fixed known-good baseline (byte-identical)"
    - "The full pre-existing suite (400+ tests) stays green after the Plan 01 refactor (CONTRACT-05 no regressions)"
  artifacts:
    - "tests/testthat/helper-mock-data.R (degenerate data factories: insufficient-obs, zero-variance)"
    - "tests/testthat/test_contract.R (strict/lenient/switching/CONTRACT-05 regression)"
    - "tests/testthat/test_parameter_set.R (degenerate_handling field validation tests)"
    - "man/degenerate-input-contract.Rd + updated NAMESPACE via devtools::document()"
  key_links:
    - "helper factories -> test_contract.R degenerate cases -> MarketModel$fit + run_event_study end-to-end (event_id/firm_symbol reach the message)"
    - "devtools::document() -> man/degenerate-input-contract.Rd -> ?degenerate-input-contract findable"
---

<objective>
Lock the Phase 1 contract with a durable regression net and make the contract doc topic installable. Add degenerate-data factories, author `test_contract.R` covering strict/lenient/mode-switching and the CONTRACT-05 byte-identical baseline, extend `test_parameter_set.R` for field validation, and regenerate `man/` + `NAMESPACE` via roxygen2.

Purpose: The five phase success criteria must be provably enforced and remain green; downstream phases inherit both the pattern and its test template.
Output: new `test_contract.R`, extended `helper-mock-data.R` and `test_parameter_set.R`, generated `man/degenerate-input-contract.Rd`, updated `NAMESPACE`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-contract-foundation/01-CONTEXT.md
@.planning/phases/01-contract-foundation/01-RESEARCH.md
@.planning/phases/01-contract-foundation/01-01-SUMMARY.md

@tests/testthat/helper-mock-data.R
@tests/testthat/test_models.R
@tests/testthat/test_parameter_set.R
</context>

<artifacts_this_phase_produces>
- `create_degenerate_model_data_insufficient(n_valid, n_event)` and `create_degenerate_model_data_zero_variance(n_event)` in helper-mock-data.R
- `tests/testthat/test_contract.R` (new): CONTRACT-01..05 assertions
- degenerate_handling validation tests appended to `tests/testthat/test_parameter_set.R`
- `man/degenerate-input-contract.Rd` (generated)
- regenerated `NAMESPACE` (helpers stay unexported via @noRd; no new export expected)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Degenerate data factories + contract test suite (strict/lenient/switching/CONTRACT-05)</name>
  <files>tests/testthat/helper-mock-data.R, tests/testthat/test_contract.R, tests/testthat/test_parameter_set.R</files>
  <read_first>
    - tests/testthat/helper-mock-data.R:137-152 (create_mock_model_data — base fixture to derive degenerate variants from)
    - tests/testthat/test_models.R:1-80 (existing MarketModel test patterns: fit, is_fitted, abnormal_returns NA path)
    - tests/testthat/test_parameter_set.R (existing field/validation test style)
    - .planning/phases/01-contract-foundation/01-01-SUMMARY.md (actual authored helper signatures and message formats — use the real message substrings)
    - R/contract.R and R/models.R MarketModel$fit (the message text and guard thresholds to assert against)
  </read_first>
  <behavior>
    - CONTRACT-02: ParameterSet$new() default -> resolved mode "lenient"; ParameterSet$new(degenerate_handling="strict") -> "strict"; options(eventstudy.degenerate_handling="strict") with NULL field -> "strict"; ParameterSet$new(degenerate_handling="bogus") errors (match.arg).
    - CONTRACT-03 (strict): MarketModel with degenerate_mode "strict" on insufficient-obs input raises an error whose message contains the event_id AND firm_symbol AND "MarketModel"; same for zero-variance input.
    - CONTRACT-04 (lenient): MarketModel with degenerate_mode "lenient" on the same input sets is_fitted FALSE, abnormal_returns(...) returns all-NA, and fit emits exactly one warning (count warnings via withCallingHandlers, assert length == 1).
    - CONTRACT-04 no-duplicate at pipeline level: run_event_study() with a degenerate firm in lenient mode emits one warning for that (event_id, firm_symbol), not several.
    - CONTRACT-05 (regression): fit MarketModel on create_mock_model_data() in lenient mode; assert alpha, beta, sigma, degree_of_freedom equal a fixed baseline captured as literal expected numbers (df == 118) using expect_equal with tolerance for the floats.
    - Mode switching without reload: within one session, ParameterSet$new(degenerate_handling="strict") then ParameterSet$new(degenerate_handling="lenient") both resolve correctly; toggling options(eventstudy.degenerate_handling=) flips resolution live.
  </behavior>
  <action>
Append two factories to tests/testthat/helper-mock-data.R:
- `create_degenerate_model_data_insufficient(n_valid = 1, n_event = 11)`: build from create_mock_model_data(), find estimation rows, set all but the first n_valid estimation rows' firm_returns and index_returns to NA_real_.
- `create_degenerate_model_data_zero_variance(n_event = 11)`: build from create_mock_model_data(), set all estimation-window index_returns to a single constant (e.g. 0.001) so sd == 0.

Create tests/testthat/test_contract.R with test_that blocks mapping to the behaviors above. For the strict tests use `expect_error(m$fit(d), regexp = "EVT_ID_HERE")` style with the real event_id/firm_symbol you set on the model, and add separate expect_error calls asserting the firm_symbol and "MarketModel" substrings appear (or a single regexp requiring all). For the lenient one-warning assertion, wrap fit() in withCallingHandlers collecting warnings into a vector and `expect_length(warnings, 1)`. For CONTRACT-05, hard-code the expected alpha/beta/sigma from the seeded fixture (set.seed(42) is inside create_mock_model_data so values are deterministic) and `expect_equal(s$alpha, <literal>, tolerance = 1e-8)` etc.; assert `s$degree_of_freedom == 118`. Restore any options() you set using `withr::local_options()` or an on.exit(options(...)) so global option state does not leak between tests (testthat 3e).

Append to tests/testthat/test_parameter_set.R: a test that ParameterSet$new(degenerate_handling="strict")$degenerate_handling == "strict", that the default field is NULL, and that ParameterSet$new(degenerate_handling="Lenient") (wrong case) or "bogus" raises an error via match.arg.

Do NOT weaken any existing assertion. Do NOT restate a literal that a negative-grep test checks inside a comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test_contract.R", reporter="summary")' 2>&1 | tee /tmp/claude-1000/-home-simonm-projects-datascience-eventstudy/2b1921d5-2069-47a4-ab26-c9758e1e0bac/scratchpad/contract_test.log; Rscript -e 'q(status = if (any(grepl("FAIL|Error", readLines("/tmp/claude-1000/-home-simonm-projects-datascience-eventstudy/2b1921d5-2069-47a4-ab26-c9758e1e0bac/scratchpad/contract_test.log")))) 1L else 0L)'</automated>
    <fails_when>Any test in test_contract.R fails or errors — specifically: strict-mode error missing event_id/firm_symbol/component, lenient-mode not setting is_fitted FALSE or emitting !=1 warning, mode resolution not honoring field>option>default, or CONTRACT-05 baseline numbers not matching (df != 118 or coefficients drifted).</fails_when>
  </verify>
  <done>test_contract.R and the parameter_set additions pass: strict names event_id/firm_symbol/component; lenient sets is_fitted FALSE with NA downstream and exactly one warning; mode switches live via field and option; CONTRACT-05 baseline matches with df=118.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/NAMESPACE and confirm full suite green + no new check NOTEs</name>
  <files>NAMESPACE, man/degenerate-input-contract.Rd</files>
  <read_first>
    - R/contract.R (the @name degenerate-input-contract topic authored in Plan 01 that must generate an Rd)
    - .planning/phases/01-contract-foundation/01-01-SUMMARY.md (confirm no @export was added to helpers)
  </read_first>
  <action>
Run `devtools::document()` to regenerate NAMESPACE and man/. Confirm `man/degenerate-input-contract.Rd` is created (the doc-only NULL topic must produce it) and that NAMESPACE gains NO new export for the internal helpers (.resolve_degenerate_mode / .handle_degenerate are @noRd). Then run the FULL existing testthat suite to confirm the Plan 01 refactor introduced no regressions across the 400+ pre-existing tests. If document() reports a roxygen warning about the doc-only topic (RESEARCH Assumption A1 risk), fix by ensuring the topic has @title and @name and is attached to a bare `NULL` — do not add @export. Do not modify unrelated Rd files beyond what document() regenerates.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::document(); stopifnot(file.exists("man/degenerate-input-contract.Rd")); ns <- readLines("NAMESPACE"); stopifnot(!any(grepl("resolve_degenerate_mode|handle_degenerate", ns)))' && Rscript -e 'res <- devtools::test(reporter = "summary"); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0, sum(df$error) == 0)'</automated>
    <fails_when>man/degenerate-input-contract.Rd is not generated, OR an internal helper leaks into NAMESPACE as an export, OR any test in the full suite fails or errors (a pre-existing test broke = CONTRACT-05 regression).</fails_when>
  </verify>
  <done>?degenerate-input-contract Rd exists; internal helpers stay unexported; the full 400+ test suite passes with zero failures/errors after the refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No applicable threats (internal statistical library, no untrusted-input boundary; this plan adds tests and regenerated docs only).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | degenerate_handling field validation coverage | low | mitigate | test_parameter_set.R asserts match.arg rejects invalid mode strings, locking the V5 input-validation control from Plan 01 |
</threat_model>

<verification>
- `testthat::test_file("tests/testthat/test_contract.R")` passes all assertions.
- `devtools::document()` produces man/degenerate-input-contract.Rd with no new NAMESPACE export for helpers.
- `devtools::test()` full suite: 0 failures, 0 errors (400+ pre-existing tests green).
</verification>

<success_criteria>
- CONTRACT-01: ?degenerate-input-contract Rd generated and findable.
- CONTRACT-02/03/04: test_contract.R proves resolution order, strict naming, lenient one-warning/NA/is_fitted, and live mode switching.
- CONTRACT-05: fixed-baseline regression (df=118, coefficients match) plus full-suite-green confirm valid-input behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-contract-foundation/01-02-SUMMARY.md` when done.
</output>
