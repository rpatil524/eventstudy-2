---
phase: 01-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/testthat/helper-mock-data.R
  - tests/testthat/test_contract.R
  - tests/testthat/test_parameter_set.R
  - NAMESPACE
  - man/degenerate-input-contract.Rd
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-03, CONTRACT-04, CONTRACT-05]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "?degenerate-input-contract Rd file is generated by roxygen2 and installs cleanly (CONTRACT-01 findable)"
    - "test_contract.R proves strict-mode error names event_id/firm_symbol for BOTH insufficient-obs and zero-variance, lenient-mode is_fitted FALSE + NA downstream + exactly one warning, and mode switching via field and via option both work without reload (CONTRACT-02/03/04)"
    - "A PIPELINE-level test asserts run_event_study() on a degenerate firm in lenient mode emits exactly one warning for that (event_id, firm_symbol) — no duplicate across fit/abnormal_returns/stats (CONTRACT-04)"
    - "A CONTRACT-05 regression asserts post-refactor valid-input MarketModel statistics equal the committed contract05_baseline.rds within 1e-8 (real before/after)"
    - "The full pre-existing suite (400+ tests) stays green after the Plan 01 refactor (CONTRACT-05 no regressions)"
  artifacts:
    - "tests/testthat/helper-mock-data.R (degenerate data factories: insufficient-obs, zero-variance)"
    - "tests/testthat/test_contract.R (unit strict/lenient for both conditions, pipeline one-warning, mode switching, CONTRACT-05 baseline regression)"
    - "tests/testthat/test_parameter_set.R (degenerate_handling field validation tests)"
    - "man/degenerate-input-contract.Rd + updated NAMESPACE via devtools::document()"
  key_links:
    - "helper factories -> test_contract.R degenerate cases -> MarketModel$fit + run_event_study end-to-end (event_id/firm_symbol reach the message)"
    - "devtools::document() -> man/degenerate-input-contract.Rd -> ?degenerate-input-contract findable"
---

<objective>
Lock the Phase 1 contract with a durable regression net and make the contract doc topic installable. Add degenerate-data factories, author `test_contract.R` covering strict/lenient/mode-switching and the CONTRACT-05 byte-identical baseline, extend `test_parameter_set.R` for field validation, and regenerate `man/` + `NAMESPACE` via roxygen2.

Purpose: The five phase success criteria must be provably enforced and remain green; downstream phases inherit both the pattern and its test template.
Output: new `test_contract.R`, extended `helper-mock-data.R` and `test_parameter_set.R`, generated `man/degenerate-input-contract.Rd`, updated `NAMESPACE`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-contract-foundation/01-CONTEXT.md
@.planning/phases/01-contract-foundation/01-RESEARCH.md
@.planning/phases/01-contract-foundation/01-01-SUMMARY.md

@tests/testthat/helper-mock-data.R
@tests/testthat/test_models.R
@tests/testthat/test_parameter_set.R
</context>

<artifacts_this_phase_produces>
- `create_degenerate_model_data_insufficient(n_valid, n_event)` and `create_degenerate_model_data_zero_variance(n_event)` in helper-mock-data.R
- `tests/testthat/test_contract.R` (new): CONTRACT-01..05 assertions, incl. unit-level strict/lenient for BOTH degenerate conditions, a PIPELINE-level one-warning test via run_event_study(), and a CONTRACT-05 regression against the committed baseline .rds
- degenerate_handling validation tests appended to `tests/testthat/test_parameter_set.R`
- `man/degenerate-input-contract.Rd` (generated)
- regenerated `NAMESPACE` (helpers stay unexported via @noRd; no new export expected)

Consumes (from Plan 01): `tests/testthat/fixtures/contract05_baseline.rds` as the CONTRACT-05 before/after reference.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Degenerate data factories + contract test suite (strict/lenient/switching/CONTRACT-05)</name>
  <files>tests/testthat/helper-mock-data.R, tests/testthat/test_contract.R, tests/testthat/test_parameter_set.R</files>
  <read_first>
    - tests/testthat/helper-mock-data.R:137-152 (create_mock_model_data — base fixture to derive degenerate variants from)
    - tests/testthat/test_models.R:1-80 (existing MarketModel test patterns: fit, is_fitted, abnormal_returns NA path)
    - tests/testthat/test_parameter_set.R (existing field/validation test style)
    - .planning/phases/01-contract-foundation/01-01-SUMMARY.md (actual authored helper signatures and message formats — use the real message substrings)
    - R/contract.R and R/models.R MarketModel$fit (the message text and guard thresholds to assert against)
    - tests/testthat/fixtures/contract05_baseline.rds (frozen pre-refactor baseline from Plan 01 Task 1 — the CONTRACT-05 reference; test_path("fixtures","contract05_baseline.rds"))
  </read_first>
  <behavior>
    - CONTRACT-02: ParameterSet$new() default -> resolved mode "lenient"; ParameterSet$new(degenerate_handling="strict") -> "strict"; options(eventstudy.degenerate_handling="strict") with NULL field -> "strict"; ParameterSet$new(degenerate_handling="bogus") errors (match.arg).
    - CONTRACT-03 (strict): MarketModel with degenerate_mode "strict" on insufficient-obs input raises an error whose message contains the event_id AND firm_symbol AND "MarketModel"; same for zero-variance input.
    - CONTRACT-04 (lenient): MarketModel with degenerate_mode "lenient" on the same input sets is_fitted FALSE, abnormal_returns(...) returns all-NA, and fit emits exactly one warning (count warnings via withCallingHandlers, assert length == 1).
    - CONTRACT-04 no-duplicate at PIPELINE level (warning 8): run_event_study() on a task containing a degenerate firm in lenient mode emits EXACTLY ONE warning for that (event_id, firm_symbol) across the whole pipeline call (collect all warnings via withCallingHandlers, filter to the ones naming that event_id+firm_symbol, assert length == 1 — no duplicate from fit vs abnormal_returns vs stats).
    - CONTRACT-05 (regression against committed baseline): load tests/testthat/fixtures/contract05_baseline.rds (frozen by Plan 01 Task 1), re-fit MarketModel on create_mock_model_data() in lenient mode, and expect_equal each captured statistic (alpha, beta, sigma, degree_of_freedom==118, r2, f_stat, p-values, first 5 FEC, first 5 abnormal returns) to the baseline with tolerance 1e-8. Do NOT hard-code literals — read them from the committed .rds so the test is a genuine before/after.
    - Mode switching without reload: within one session, ParameterSet$new(degenerate_handling="strict") then ParameterSet$new(degenerate_handling="lenient") both resolve correctly; toggling options(eventstudy.degenerate_handling=) flips resolution live.
  </behavior>
  <action>
Append two factories to tests/testthat/helper-mock-data.R:
- `create_degenerate_model_data_insufficient(n_valid = 1, n_event = 11)`: build from create_mock_model_data(), find estimation rows, set all but the first n_valid estimation rows' firm_returns and index_returns to NA_real_.
- `create_degenerate_model_data_zero_variance(n_event = 11)`: build from create_mock_model_data(), set all estimation-window index_returns to a single constant (e.g. 0.001) so sd == 0.

Create tests/testthat/test_contract.R with test_that blocks mapping to the behaviors above. For the strict tests use `expect_error(m$fit(d), regexp = ...)` with the real event_id/firm_symbol you set on the model, asserting the event_id, firm_symbol, and "MarketModel" substrings all appear (a single regexp requiring all, or separate expect_error calls). Cover BOTH degenerate conditions (insufficient obs AND zero variance) in strict mode. For the lenient one-warning assertion, wrap fit() in withCallingHandlers collecting warnings into a vector and `expect_length(warnings, 1)`, plus `expect_false(m$is_fitted)` and `expect_true(all(is.na(m$abnormal_returns(d)$abnormal_returns)))`.

Add the PIPELINE-level CONTRACT-04 test (warning 8): build a small multi-firm task with ONE degenerate firm (use create_mock_task-style helpers plus a degenerate firm, or the existing task factory with a NA'd estimation window for one firm), run run_event_study(task, ParameterSet$new()) inside withCallingHandlers collecting ALL warnings, then assert exactly one collected warning names that firm's (event_id, firm_symbol) — proving no duplicate warning is emitted across fit/abnormal_returns/statistics for a single degenerate event.

For CONTRACT-05, load `baseline <- readRDS(testthat::test_path("fixtures", "contract05_baseline.rds"))`, fit MarketModel on create_mock_model_data() in lenient mode, and `expect_equal(s$alpha, baseline$alpha, tolerance = 1e-8)` for each captured field (alpha, beta, sigma, degree_of_freedom == baseline$df == 118, r2, f_stat, pval_alpha, pval_beta, head(FEC,5) == baseline$fec_head, head(abnormal_returns,5) == baseline$ar_head). Do NOT hard-code numeric literals and do NOT regenerate the baseline in this test — the committed .rds is the frozen reference.

Restore any options() you set using `withr::local_options()` or an on.exit(options(...)) so global option state does not leak between tests (testthat 3e).

Append to tests/testthat/test_parameter_set.R: a test that ParameterSet$new(degenerate_handling="strict")$degenerate_handling == "strict", that the default field is NULL, and that ParameterSet$new(degenerate_handling="Lenient") (wrong case) or "bogus" raises an error via match.arg.

Do NOT weaken any existing assertion. Do NOT restate a literal that a negative-grep test checks inside a comment.
  </action>
  <verify>
    <automated>test -f tests/testthat/test_contract.R || { echo "MISSING tests/testthat/test_contract.R"; exit 1; }; test -f tests/testthat/fixtures/contract05_baseline.rds || { echo "MISSING contract05_baseline.rds (Plan 01 Task 1 must run first)"; exit 1; }; Rscript -e 'devtools::load_all("."); r <- as.data.frame(testthat::test_file("tests/testthat/test_contract.R", reporter="silent")); stopifnot(sum(r$failed) == 0, all(r$error == FALSE))'</automated>
    <fails_when>tests/testthat/test_contract.R or the CONTRACT-05 baseline .rds is absent, OR any test in test_contract.R fails or errors — specifically: strict-mode error missing event_id/firm_symbol/component (either degenerate condition), lenient-mode not setting is_fitted FALSE or emitting !=1 warning at unit OR pipeline level, mode resolution not honoring field>option>default, or CONTRACT-05 values not matching the committed baseline within 1e-8 (df != 118 or any coefficient drifted).</fails_when>
  </verify>
  <done>test_contract.R and the parameter_set additions pass: strict names event_id/firm_symbol/component; lenient sets is_fitted FALSE with NA downstream and exactly one warning; mode switches live via field and option; CONTRACT-05 baseline matches with df=118.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/NAMESPACE and confirm full suite green + no new check NOTEs</name>
  <files>NAMESPACE, man/degenerate-input-contract.Rd</files>
  <read_first>
    - R/contract.R (the @name degenerate-input-contract topic authored in Plan 01 that must generate an Rd)
    - .planning/phases/01-contract-foundation/01-01-SUMMARY.md (confirm no @export was added to helpers)
  </read_first>
  <action>
Run `devtools::document()` to regenerate NAMESPACE and man/. Confirm `man/degenerate-input-contract.Rd` is created (the doc-only NULL topic must produce it) and that NAMESPACE gains NO new export for the internal helpers (.resolve_degenerate_mode / .handle_degenerate are @noRd). Then run the FULL existing testthat suite to confirm the Plan 01 refactor introduced no regressions across the 400+ pre-existing tests. If document() reports a roxygen warning about the doc-only topic (RESEARCH Assumption A1 risk), fix by ensuring the topic has @title and @name and is attached to a bare `NULL` — do not add @export. Do not modify unrelated Rd files beyond what document() regenerates.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::document()'</automated>
    <fails_when>devtools::document() errors or emits a roxygen warning about the degenerate-input-contract topic (fix: ensure @name + @title on a bare NULL, no @export).</fails_when>
    <automated>Rscript -e 'stopifnot(file.exists("man/degenerate-input-contract.Rd"))'</automated>
    <fails_when>man/degenerate-input-contract.Rd was not generated by document() — the doc-only NULL topic did not produce an Rd (CONTRACT-01 not findable).</fails_when>
    <automated>Rscript -e 'ns <- readLines("NAMESPACE"); stopifnot(!any(grepl("resolve_degenerate_mode|handle_degenerate", ns)))'</automated>
    <fails_when>An internal helper (.resolve_degenerate_mode / .handle_degenerate) leaked into NAMESPACE as an export — they must stay @noRd/unexported.</fails_when>
    <automated>Rscript -e 'df <- as.data.frame(devtools::test(reporter = "silent")); stopifnot(sum(df$failed) == 0, sum(df$error) == 0)'</automated>
    <fails_when>Any test in the full suite fails or errors — a pre-existing test broke, meaning the Plan 01 refactor regressed valid-input behavior (CONTRACT-05 / TEST-03).</fails_when>
  </verify>
  <done>?degenerate-input-contract Rd exists; internal helpers stay unexported; the full 400+ test suite passes with zero failures/errors after the refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No applicable threats (internal statistical library, no untrusted-input boundary; this plan adds tests and regenerated docs only).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | degenerate_handling field validation coverage | low | mitigate | test_parameter_set.R asserts match.arg rejects invalid mode strings, locking the V5 input-validation control from Plan 01 |
</threat_model>

<verification>
- test_contract.R present + baseline .rds present (guarded) then all assertions pass: strict names event_id/firm_symbol for both conditions; lenient one-warning/NA/is_fitted at unit AND pipeline level; live mode switching; CONTRACT-05 equals committed baseline within 1e-8.
- `devtools::document()` produces man/degenerate-input-contract.Rd with no new NAMESPACE export for helpers (each check its own sequential step).
- `devtools::test()` full suite: 0 failures, 0 errors (400+ pre-existing tests green).
</verification>

<success_criteria>
- CONTRACT-01: ?degenerate-input-contract Rd generated and findable.
- CONTRACT-02/03/04: test_contract.R proves resolution order, strict naming (both conditions), lenient one-warning/NA/is_fitted at unit and pipeline level, and live mode switching.
- CONTRACT-05: baseline regression against committed contract05_baseline.rds (df=118, coefficients match within 1e-8) plus full-suite-green confirm valid-input behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-contract-foundation/01-02-SUMMARY.md` when done.
</output>
