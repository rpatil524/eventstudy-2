---
phase: 08-agent-skill-waitlist-green-check-gate
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - DESCRIPTION
  - NEWS.md
  - .Rbuildignore
  - man/advisor_pro.Rd
  - NAMESPACE
autonomous: true
requirements: [CRAN-04, BIZ-01]
estimate:
  tokens: 45000
  raw_tokens: 27000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "DESCRIPTION Version is bumped 0.50.0 → 0.60.0 to match the v0.60.0 milestone"
    - "NEWS.md has a v0.60.0 section summarizing the advisor feature set shipped across Phases 5–8 (BIZ-01: mentions the Advisor Pro waitlist)"
    - ".Rbuildignore excludes .claude, .planning, and .gsd (and other dev-only top-level dirs) so those artifacts ship in the repo but never enter the CRAN tarball"
    - "roxygen docs are regenerated so man/advisor_pro.Rd exists and NAMESPACE reflects any new @export (there should be none new — footer helper is @noRd, advisor_pro is docs-only)"
    - "R CMD check --as-cran reports no new NOTEs/WARNINGs vs the current baseline (CRAN-04)"
    - "The full existing testthat suite stays green — no hardcoded count; the criterion is existing tests pass (CRAN-04)"
  artifacts:
    - "DESCRIPTION (Version: 0.60.0)"
    - "NEWS.md (# EventStudy 0.60.0 section)"
    - ".Rbuildignore (entries for .planning and .gsd added; .claude already present)"
    - "man/advisor_pro.Rd (generated)"
  key_links:
    - "The check gate runs against the state AFTER 08-01 (skill + footer + advisor_pro topic present), so .Rbuildignore must exclude dev dirs to avoid a non-standard-top-level-file NOTE"
    - "roxygen document() must run before R CMD check so man/advisor_pro.Rd and NAMESPACE are current"
---

<objective>
Ship the v0.60.0 milestone CRAN-clean: bump the package version, write the NEWS entries, keep dev-only artifacts out of the CRAN tarball via `.Rbuildignore`, regenerate docs, and prove a green `R CMD check --as-cran` with the existing test suite still passing.

Purpose: close the milestone's ship gate and confirm the deferred Phase-6 CRAN-02 concern is clean.
Output: DESCRIPTION version bump, NEWS.md v0.60.0 section, `.Rbuildignore` dev-dir exclusions, regenerated `man/advisor_pro.Rd`, and a passing `R CMD check --as-cran`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-skill-waitlist-green-check-gate/08-CONTEXT.md
@.planning/phases/08-agent-skill-waitlist-green-check-gate/08-01-PLAN.md

# Real current state (verified during planning):
@DESCRIPTION
@.Rbuildignore
@NEWS.md
</context>

<artifacts_this_phase_produces>
New/changed by THIS plan:
- `DESCRIPTION` Version: 0.50.0 → 0.60.0 (leave URL/BugReports/Date per Claude's discretion; do not add new deps)
- `NEWS.md` new top section `# EventStudy 0.60.0` summarizing the Grounded AI Advisor feature set (Phases 5–8): es_diagnostics + KB, provider abstraction, es_advise + grounding guard, generate_report(advice=), the Agent Skill, and the Advisor Pro waitlist (BIZ-01)
- `.Rbuildignore` new entries: `^\.planning$`, `^\.gsd$` (and any other dev-only top-level dir); `^\.claude$` is ALREADY present — do not duplicate
- `man/advisor_pro.Rd` (roxygen-generated from R/advisor_pro.R created in 08-01)
- possibly regenerated `NAMESPACE` (expected: no new exports — footer helper is @noRd, advisor_pro is docs-only)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + NEWS.md v0.60.0 + .Rbuildignore dev-dir exclusions + regenerate docs</name>
  <files>DESCRIPTION, NEWS.md, .Rbuildignore, man/advisor_pro.Rd, NAMESPACE</files>
  <read_first>
    - DESCRIPTION line 4 (Version: 0.50.0 — bump to 0.60.0) and line 26 (URL for the NEWS/waitlist reference)
    - .Rbuildignore (current entries — `^\.claude$` already present at last line; .planning and .gsd are NOT yet listed)
    - NEWS.md lines 1-30 (existing `# EventStudy 0.50.0` heading + bullet style to mirror)
    - .planning/ROADMAP.md Phase 5–8 sections (feature set to summarize in NEWS)
    - R/advisor_pro.R (created in 08-01 — the source of man/advisor_pro.Rd)
  </read_first>
  <action>
    1. DESCRIPTION: change `Version: 0.50.0` to `Version: 0.60.0`. Do NOT add or move any dependency; httr2/jsonlite stay in Suggests. Date field update is Claude's discretion.

    2. NEWS.md: prepend a new `# EventStudy 0.60.0` section ABOVE the existing 0.50.0 section, with a short "Grounded AI Advisor" summary and bullets covering the advisor feature set delivered across Phases 5–8: offline `es_diagnostics()` + grounding knowledge base (`es_kb`), the LLM-agnostic provider abstraction (`provider()`, Anthropic/OpenAI-compatible/custom), grounded `es_advise()` with the runtime grounding guard, `generate_report(advice=)` integration, the Claude Code Agent Skill (`.claude/skills/es-advisor/`), and the passive "Advisor Pro" waitlist with an opt-in `options(eventstudy.advisor_pro_footer = TRUE)` footer (BIZ-01). Mirror the existing NEWS heading/bullet style. Do NOT hardcode a test count.

    3. .Rbuildignore: append `^\.planning$` and `^\.gsd$` (and any other dev-only top-level dir present, e.g. `^\.scratch$` if it exists). Do NOT re-add `^\.claude$` — it is already present. These keep the skill/planning artifacts in the repo but out of the CRAN tarball (avoids a non-standard-top-level-file NOTE).

    4. Regenerate documentation so man/advisor_pro.Rd exists and NAMESPACE is current: run `devtools::document(".")`. Confirm NAMESPACE gained no unexpected exports (footer helper is @noRd; advisor_pro is docs-only).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'devtools::document("."); d <- read.dcf("DESCRIPTION"); stopifnot(d[,"Version"] == "0.60.0"); stopifnot(any(grepl("0\\.60\\.0", readLines("NEWS.md")))); rb <- readLines(".Rbuildignore"); stopifnot(any(grepl("planning", rb)), any(grepl("gsd", rb)), any(grepl("claude", rb))); stopifnot(file.exists("man/advisor_pro.Rd")); cat("GATE1_OK\n")'</automated>
    <fails_when>Version is not 0.60.0, NEWS lacks a 0.60.0 line, .Rbuildignore is missing a planning/gsd/claude entry, or man/advisor_pro.Rd was not generated — a stopifnot fails, no `GATE1_OK`, exit non-zero.</fails_when>
  </verify>
  <acceptance_criteria>
    - DESCRIPTION Version == 0.60.0; no new dependency added
    - NEWS.md has a `# EventStudy 0.60.0` section summarizing the advisor feature set incl. the Advisor Pro waitlist (BIZ-01)
    - .Rbuildignore excludes .claude (already), .planning, .gsd (and other dev-only top-level dirs)
    - man/advisor_pro.Rd generated; NAMESPACE has no unexpected new exports
  </acceptance_criteria>
  <done>The package is versioned 0.60.0 with milestone NEWS, dev artifacts are build-ignored, and the ?advisor_pro man page is generated.</done>
  <reversibility rating="reversible">Version/NEWS/.Rbuildignore edits are trivially revertable; no data or API change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Green R CMD check --as-cran gate + full test suite green</name>
  <files>cran-comments.md</files>
  <read_first>
    - DESCRIPTION (final dependency layout — httr2/jsonlite in Suggests, requireNamespace-guarded)
    - .Rbuildignore (final exclusions from Task 1)
    - .planning/STATE.md "Pending Todos" (Phase 6 deferred CRAN-02: this gate confirms clean)
    - cran-comments.md if present (existing check-notes baseline to compare against)
  </read_first>
  <action>
    Run the CRAN check gate against the post-08-01 + post-Task-1 state.

    1. Full test suite: `Rscript -e 'devtools::test()'` — must be green (CRAN-04: existing tests stay green; do NOT hardcode a count, the operative criterion is "no failures").

    2. `R CMD check --as-cran`: run `Rscript -e 'devtools::check(args = "--as-cran", error_on = "warning")'` (or `rcmdcheck::rcmdcheck(args = "--as-cran")` if devtools delegates to it). Compare the result against the current baseline: there must be NO new NOTEs and NO new WARNINGs versus the pre-milestone check. Pre-existing NOTEs unrelated to this milestone (if any documented in cran-comments.md) are acceptable ONLY if unchanged — a genuinely NEW note/warning fails the gate.

    3. If the check surfaces a new NOTE caused by the skill/planning artifacts (non-standard top-level files), confirm the `.Rbuildignore` entries from Task 1 cover it; if a dev dir was missed, add it and re-run. If a new note is caused by the `?advisor_pro` doc topic or the footer helper, fix at the source (roxygen tags / @noRd) and re-run — do NOT suppress by hand-editing generated files.

    4. Record the outcome in cran-comments.md (create or update): note "0 errors, 0 warnings, N notes (all pre-existing / none new)" and that the Phase-6-deferred CRAN-02 network-safety concern is confirmed clean (no network in examples/tests/vignettes by default).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'devtools::test()' && Rscript -e 'res <- rcmdcheck::rcmdcheck(args = "--as-cran", error_on = "never", quiet = TRUE); cat("ERRORS:", length(res$errors), "WARNINGS:", length(res$warnings), "NOTES:", length(res$notes), "\n"); if (length(res$errors) > 0 || length(res$warnings) > 0) { cat(res$errors, res$warnings, sep="\n"); quit(status=1) }; cat("CHECK_GATE_OK\n")'</automated>
    <fails_when>Any test fails, OR R CMD check reports ≥1 ERROR or ≥1 WARNING — the command prints the error/warning text and exits non-zero (no `CHECK_GATE_OK`). New NOTEs must be reviewed against baseline; a milestone-caused new NOTE also fails the gate on review.</fails_when>
  </verify>
  <acceptance_criteria>
    - `devtools::test()` full suite green (no failures; no hardcoded count)
    - `R CMD check --as-cran`: 0 errors, 0 warnings, and no NEW notes vs baseline (CRAN-04)
    - cran-comments.md records the clean result and confirms CRAN-02 network-safety
    - Any milestone-introduced note (skill dirs, ?advisor_pro, footer) is fixed at the source and .Rbuildignore covers dev dirs
  </acceptance_criteria>
  <done>The v0.60.0 package passes `R CMD check --as-cran` with no new NOTEs/WARNINGs and the existing test suite green — the milestone ship gate is met.</done>
  <reversibility rating="reversible">Check is read-only aside from cran-comments.md notes; no product change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CRAN tarball | Dev-only artifacts (.claude/.planning/.gsd) must not enter the shipped tarball |
| package → network (check-time) | Examples/tests/vignettes must not touch the network under --as-cran |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | CRAN tarball contents | medium | mitigate | .Rbuildignore excludes .claude/.planning/.gsd so no dev/planning artifacts (or any inadvertent secrets) ship; verified by the check-gate NOTE review (Task 2) |
| T-08-05 | Tampering | generated man/NAMESPACE | low | mitigate | Regenerate via roxygen document() rather than hand-editing; check gate catches drift |
| T-08-SC | Tampering | dependency surface | low | accept | No package installs in this plan; DESCRIPTION deps unchanged (httr2/jsonlite remain Suggests). Package-legitimacy gate not applicable — no new packages introduced. |
</threat_model>

<verification>
- DESCRIPTION Version == 0.60.0; NEWS.md has 0.60.0 section; .Rbuildignore excludes dev dirs; man/advisor_pro.Rd generated (Task 1)
- `devtools::test()` green; `R CMD check --as-cran` 0 errors / 0 warnings / no new notes (Task 2)
- cran-comments.md records the clean result
</verification>

<success_criteria>
- CRAN-04: green `R CMD check` with no new NOTEs/WARNINGs and the existing test suite green
- BIZ-01: NEWS.md advertises the Advisor Pro waitlist as part of the shipped feature set
</success_criteria>

<output>
Create `.planning/phases/08-agent-skill-waitlist-green-check-gate/08-02-SUMMARY.md` when done
</output>
