---
phase: 08-agent-skill-waitlist-green-check-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/es-advisor/SKILL.md
  - .claude/skills/es-advisor/reference/workflow.md
  - .claude/skills/es-advisor/reference/function-map.md
  - .claude/skills/es-advisor/reference/interpreting-diagnostics.md
  - R/advisor_pro.R
  - R/advise.R
  - R/advise_offline.R
  - README.md
  - tests/testthat/test_advisor_pro_footer.R
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, BIZ-01, BIZ-02]
estimate:
  tokens: 70000
  raw_tokens: 42000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A Claude Code user can follow .claude/skills/es-advisor/SKILL.md to run the full load→run→diagnose→advise→re-run→compare loop using only already-exported functions (SKILL-01)"
    - "Every function name/arg cited in the skill reference files resolves to a real export in NAMESPACE — no invented symbols (SKILL-02)"
    - "The skill documents a no-API-key degrade path using es_diagnostics() + offline es_advise()/recommend_stat()/flag_robustness() that never errors for lack of a key (SKILL-03)"
    - "Docs advertise a future retrieval-grounded 'Advisor Pro' paid tier via a ?advisor_pro help topic and a README section (BIZ-01)"
    - "The Advisor Pro waitlist footer is silent by default and prints (a static URL, no network) only when options(eventstudy.advisor_pro_footer = TRUE) — appended to print.Advice and print.es_advice only, never at attach/startup (BIZ-02)"
    - "Enabling the footer initiates zero network/telemetry — it emits a static string only (BIZ-02, CRAN no-phone-home)"
  artifacts:
    - ".claude/skills/es-advisor/SKILL.md"
    - ".claude/skills/es-advisor/reference/workflow.md"
    - ".claude/skills/es-advisor/reference/function-map.md"
    - ".claude/skills/es-advisor/reference/interpreting-diagnostics.md"
    - "R/advisor_pro.R (roxygen ?advisor_pro doc topic + .advisor_pro_footer helper)"
    - "tests/testthat/test_advisor_pro_footer.R"
  key_links:
    - "print.Advice (R/advise.R) and print.es_advice (R/advise_offline.R) both call the option-guarded footer helper immediately before invisible(x)"
    - "The footer is gated by options(eventstudy.advisor_pro_footer = TRUE), default OFF"
    - "SKILL reference function-map cites only these exports: prepare_event_study, run_event_study, fit_model, calculate_statistics, es_diagnostics, es_advise, recommend_stat, flag_robustness, provider, generate_report, plot_diagnostics, model_diagnostics"
---

<objective>
Deliver the user-facing advisor surface for v0.60.0: the Claude Code Agent Skill that drives the full advisor loop with existing exported functions only, plus the passive "Advisor Pro" waitlist (a `?advisor_pro` help topic, a README section, and an opt-in, default-off printed footer on the advice print methods).

Purpose: give a Claude Code user a documented, runnable end-to-end path through the advisor, and advertise the future paid tier with a static, no-phone-home pointer.
Output: `.claude/skills/es-advisor/` (SKILL.md + reference files), `R/advisor_pro.R` (doc topic + footer helper), guarded footer calls in the two advice print methods, a README waitlist section, and a footer regression test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-skill-waitlist-green-check-gate/08-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

# Real symbols the SKILL and footer must cite (verified during planning):
@R/advise.R
@R/advise_offline.R
@NAMESPACE
</context>

<artifacts_this_phase_produces>
New symbols/files introduced by THIS plan (08-02 adds the version/NEWS/.Rbuildignore/check-gate items):
- `.claude/skills/es-advisor/SKILL.md` — Agent Skill index (frontmatter + trigger + loop overview)
- `.claude/skills/es-advisor/reference/workflow.md` — the load→run→diagnose→advise→re-run→compare step recipe (Rscript one-liners)
- `.claude/skills/es-advisor/reference/function-map.md` — exported-function reference (name → purpose → key args), citing ONLY real exports
- `.claude/skills/es-advisor/reference/interpreting-diagnostics.md` — how to read es_diagnostics fields + offline degrade path
- `R/advisor_pro.R` — new file: roxygen `?advisor_pro` doc topic (docs-only) + `.advisor_pro_footer()` `@noRd` helper
- `.advisor_pro_footer()` — internal helper returning/cat-ing the static waitlist string, option-guarded
- option `eventstudy.advisor_pro_footer` — new opt-in package option, default OFF
- footer call sites added inside `print.Advice` (R/advise.R) and `print.es_advice` (R/advise_offline.R)
- README.md `## Advisor Pro (Waitlist)` section
- `tests/testthat/test_advisor_pro_footer.R` — footer regression test
- generated: `man/advisor_pro.Rd` (roxygen output — regenerated in 08-02's document step or here)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Agent Skill — end-to-end runnable advisor-loop walkthrough (SKILL.md + reference/)</name>
  <files>.claude/skills/es-advisor/SKILL.md, .claude/skills/es-advisor/reference/workflow.md, .claude/skills/es-advisor/reference/function-map.md, .claude/skills/es-advisor/reference/interpreting-diagnostics.md</files>
  <read_first>
    - R/advise.R (es_advise signature: es_advise(diagnostics, task_type, provider = NULL, model = NULL, ...); task_type values interpret/recommend_stat/recommend_model/flag_robustness/design_discussion/report_writing)
    - R/advise_offline.R (recommend_stat(x, provider = NULL, ...), flag_robustness(x, provider = NULL, ...) — dispatch on EventStudyTask/es_diagnostics/default)
    - R/es_diagnostics.R (es_diagnostics(task, max_events = 20L))
    - R/provider.R (provider(type = NULL, fn = NULL, model = NULL, base_url = NULL, ...); 3-tier arg→env→default resolution; env vars EVENTSTUDY_ADVISOR_PROVIDER/_MODEL/_BASE_URL)
    - R/report.R (generate_report(task, ..., advice = NULL, ...))
    - R/execute.R (run_event_study(task, parameter_set); fit_model(task, parameter_set); calculate_statistics(task, parameter_set))
    - NAMESPACE (authoritative export list — cite ONLY names present here)
    - .planning/codebase/CONVENTIONS.md (snake_case public API)
  </read_first>
  <action>
    Create the Agent Skill at the SKILL-01-fixed path `.claude/skills/es-advisor/SKILL.md` plus a `reference/` subdirectory (Claude's-discretion split: workflow.md, function-map.md, interpreting-diagnostics.md).

    SKILL.md: YAML frontmatter with `name` and a `description` whose trigger wording covers running/diagnosing/advising on an event study via the EventStudy package. Body: a scannable overview of the full loop (load → run → diagnose → advise → re-run → compare) and pointers into the reference files. State plainly that the skill drives the user's R session via Rscript one-liners and adds NO new R code (SKILL-02) — it calls only already-exported package functions.

    workflow.md: the concrete step recipe as Rscript/R one-liners, each step naming the exact exported function and its real arguments:
      1. load: build/prepare a task, `prepare_event_study(task, parameter_set)`
      2. run: `run_event_study(task, parameter_set)` (or the explicit `fit_model()` → `calculate_statistics()` pair)
      3. diagnose: `es_diagnostics(task, max_events = 20L)`
      4. advise: `es_advise(diagnostics, task_type = "interpret", provider = provider(...))` and the offline `recommend_stat()` / `flag_robustness()`
      5. re-run: change model/statistic per advice, re-run the pipeline
      6. compare: contrast diagnostics/advice across runs; optionally `generate_report(task, advice = ...)`
    Include provider selection via `provider(type=, model=, base_url=)` and the 3-tier arg→env→default resolution.

    function-map.md: a table mapping each exported function used to purpose + key args. Cite ONLY these real exports (verified against NAMESPACE): prepare_event_study, run_event_study, fit_model, calculate_statistics, es_diagnostics, es_advise, recommend_stat, flag_robustness, provider, generate_report, plot_diagnostics, model_diagnostics, es_kb. Do NOT invent function names or arguments.

    interpreting-diagnostics.md: how to read the es_diagnostics fields (estimation-window fit signals, event-window results, cross-sectional signals, per-event contract state) and the SKILL-03 degrade path — when `provider()` resolves to no API key, fall back to the always-available offline path (`es_diagnostics()` + offline `es_advise()`/`recommend_stat()`/`flag_robustness()` with source=offline_kb); the skill must never error for lack of a key and never fabricate a number.

    Do NOT add any `.R` file for the skill — SKILL-02 requires no new R code for the skill itself (the footer in Task 2 is the waitlist feature, not the skill).
  </action>
  <verify>
    <automated>test -f .claude/skills/es-advisor/SKILL.md && test -d .claude/skills/es-advisor/reference && ls .claude/skills/es-advisor/reference/*.md >/dev/null 2>&1 && echo SKILL_FILES_OK</automated>
    <fails_when>Any of SKILL.md / reference dir / reference *.md files is missing — the command prints nothing (no `SKILL_FILES_OK`) and exits non-zero.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'ns <- readLines("NAMESPACE"); exp <- sub("^export\\(([^)]*)\\)$", "\\1", grep("^export\\(", ns, value=TRUE)); md <- paste(readLines(list.files(".claude/skills/es-advisor/reference", full.names=TRUE, pattern="function-map"), warn=FALSE), collapse="\n"); cited <- unique(regmatches(md, gregexpr("[a-zA-Z_][a-zA-Z0-9_.]*(?=\\()", md, perl=TRUE))[[1]]); pkg_like <- grep("^(es_|prepare_event_study|run_event_study|fit_model|calculate_statistics|recommend_stat|flag_robustness|provider|generate_report|plot_diagnostics|model_diagnostics)", cited, value=TRUE); bad <- setdiff(pkg_like, exp); if (length(bad)) { cat("UNEXPORTED_SYMBOLS_CITED:", paste(bad, collapse=", "), "\n"); quit(status=1) }; cat("FUNCTION_MAP_GROUNDED\n")'</automated>
    <fails_when>function-map.md cites a package-looking function (es_*, or a core pipeline name) that is not in NAMESPACE — prints `UNEXPORTED_SYMBOLS_CITED: ...` and exits 1.</fails_when>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists at `.claude/skills/es-advisor/SKILL.md` with frontmatter (name + trigger description) and a full-loop overview (SKILL-01)
    - reference/ contains focused files covering workflow steps, function map, and diagnostics interpretation (SKILL-02 ships reference files)
    - Every package function cited resolves to a real NAMESPACE export; no invented names/args (SKILL-02)
    - The degrade path (no API key → offline es_diagnostics + offline advice, never error, never fabricate) is documented (SKILL-03)
    - No new `.R` file added for the skill
  </acceptance_criteria>
  <done>A Claude Code user can open SKILL.md, follow the reference workflow, and execute the full load→run→diagnose→advise→re-run→compare loop with only exported functions; the no-key degrade path is documented; every cited symbol is a real export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advisor Pro waitlist — opt-in footer helper + guarded print hooks + ?advisor_pro topic + README section</name>
  <files>R/advisor_pro.R, R/advise.R, R/advise_offline.R, README.md</files>
  <read_first>
    - R/advise.R lines 605-661 (print.Advice — ends with `invisible(x)` at line 660; hook the footer immediately before it)
    - R/advise_offline.R lines 132-169 (print.es_advice — ends with `invisible(x)` at line 168; hook the footer immediately before it)
    - R/knowledge_base.R lines 1-20 (roxygen doc-block pattern for a data/doc topic — @name/@keywords style to mirror for ?advisor_pro)
    - R/EventStudy-package.R (package-doc roxygen style + globalVariables pattern)
    - .planning/codebase/CONVENTIONS.md (leading-dot @noRd internal helpers, roxygen2 docs, %||% from rlang)
    - README.md lines 1-45 (heading style + section placement)
    - DESCRIPTION URL line 26 (https://github.com/sipemu/eventstudy — anchor the static waitlist URL under this repo)
  </read_first>
  <behavior>
    - Default (option unset or FALSE): printing an Advice or es_advice object emits NO waitlist footer line — output is byte-identical to the pre-patch print (BIZ-02 silent-by-default)
    - options(eventstudy.advisor_pro_footer = TRUE): printing an Advice or es_advice object appends exactly one footer block containing the static waitlist URL, then still returns invisible(x)
    - The footer is a static string only — printing it makes NO network call and reads NO env/telemetry (CRAN no-phone-home). Assert via a mocked/blocked network expectation or by asserting the helper's body references no connection/download/httr2 call.
    - The footer never appears at package attach/startup (it lives only in the two print methods, not in .onAttach/.onLoad)
    - `?advisor_pro` resolves to a help topic advertising the future retrieval-grounded paid tier and pointing at the static waitlist URL (BIZ-01)
  </behavior>
  <action>
    Create `R/advisor_pro.R` containing two things:

    (1) A roxygen doc-only topic `advisor_pro`: a package-doc-style block (mirror the R/knowledge_base.R @name/@keywords convention) with `@name advisor_pro`, `@title Advisor Pro (future paid tier)`, and `@description` advertising the future retrieval-grounded "Advisor Pro" tier and the passive waitlist, embedding the static GitHub-hosted URL (a README anchor / GitHub Pages page under https://github.com/sipemu/eventstudy — Claude's discretion on the exact anchor). Mark it `@keywords internal` so it is a discoverable `?advisor_pro` topic without cluttering the index. This is docs-only — it defines NO runtime behavior. If roxygen requires an object to attach the doc to, attach to a NULL package-data-style stub that adds no runtime behavior; prefer the pure `@name` doc block if roxygen accepts it.

    (2) An `@noRd` internal helper `.advisor_pro_footer(con = stdout())` that: checks `isTRUE(getOption("eventstudy.advisor_pro_footer", FALSE))`; when FALSE returns invisibly with no output; when TRUE `cat()`s a short static footer block (one or two lines) containing the static waitlist URL. The helper makes NO network/connection call — pure `cat()` of a constant string. Document WHY (BIZ-02 CRAN no-phone-home) in a comment that does NOT restate any literal string a test negative-greps for.

    Then hook the helper into both print methods, immediately BEFORE their `invisible(x)` return:
    - In R/advise.R print.Advice (before line 660 `invisible(x)`): add `.advisor_pro_footer()`
    - In R/advise_offline.R print.es_advice (before line 168 `invisible(x)`): add `.advisor_pro_footer()`
    Do NOT touch print.es_diagnostics or any attach/load hook.

    README.md: add a `## Advisor Pro (Waitlist)` section (placement Claude's discretion, e.g. after the advisor/features section) advertising the future paid tier and linking the same static URL, and noting the opt-in footer option `options(eventstudy.advisor_pro_footer = TRUE)`.

    Use the existing `%||%` from rlang if needed; keep the patch minimal and guarded. Do not change any default behavior on valid input (CRAN-05 spirit).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test_advisor_pro_footer.R")'</automated>
    <fails_when>Any footer test fails (default-silent, opt-in-prints, or no-network) — testthat reports FAIL/ERROR and the process exits non-zero.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'devtools::load_all("."); on <- grep("onAttach|onLoad|advisor_pro_footer", readLines("R/EventStudy-package.R"), value=TRUE); if (length(on)) { cat("FOOTER_IN_STARTUP\n"); quit(status=1) }; cat("NO_STARTUP_FOOTER\n")'</automated>
    <fails_when>The footer helper is wired into an attach/load hook — prints `FOOTER_IN_STARTUP` and exits 1. (Footer must live only in the print methods.)</fails_when>
  </verify>
  <acceptance_criteria>
    - `R/advisor_pro.R` exists with the `advisor_pro` doc topic (BIZ-01) and the `.advisor_pro_footer()` `@noRd` helper
    - Footer is silent when `eventstudy.advisor_pro_footer` is unset/FALSE and prints the static URL only when TRUE (BIZ-02)
    - Footer helper makes zero network/connection calls (CRAN no-phone-home) — asserted by test
    - Footer is called only inside print.Advice and print.es_advice, never at attach/startup
    - README has an Advisor Pro waitlist section with the static URL and the option name
  </acceptance_criteria>
  <done>Printing an Advice/es_advice is byte-identical to before by default; setting the option appends a static waitlist URL footer with no network; `?advisor_pro` and the README advertise the future paid tier.</done>
  <reversibility rating="reversible">Docs + an opt-in default-off option; removable without data loss or migration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Footer regression test + full-suite green gate</name>
  <files>tests/testthat/test_advisor_pro_footer.R</files>
  <read_first>
    - R/advise.R (print.Advice — construct or obtain an Advice object for the test; note fields source/task_type/is_deterministic/recommendations/caveats)
    - R/advise_offline.R (print.es_advice + .build_offline_advice — how an es_advice object is shaped for the test)
    - tests/testthat/test_advise.R (Phase 07 patterns for building an Advice fixture and capturing print output via capture.output)
    - tests/testthat/test_report_advice.R (Phase 07 patterns; withr option-setting convention if used)
    - .planning/codebase/CONVENTIONS.md (testthat 3e, helper-*.R usage)
  </read_first>
  <action>
    Write `tests/testthat/test_advisor_pro_footer.R` (testthat 3e) covering:
    (a) DEFAULT SILENT: with the option unset/FALSE, `capture.output(print(advice_obj))` and `capture.output(print(es_advice_obj))` contain NO waitlist-URL line — assert the URL substring is absent.
    (b) OPT-IN PRINTS: wrap in `withr::with_options(list(eventstudy.advisor_pro_footer = TRUE), ...)`, capture print output for BOTH an Advice and an es_advice object, and assert the static waitlist URL substring IS present exactly once.
    (c) NO NETWORK: assert the footer path makes no network call — e.g. run the opt-in print inside `httr2::with_mocked_responses(function(req) stop("network was called"), ...)` if httr2 is available, OR assert `.advisor_pro_footer` body/deparse contains no connection/download/request call (`url(`, `download.file`, `httr2::request`, `curl`). Guard the httr2 branch with `skip_if_not_installed("httr2")`.
    (d) INVISIBLE RETURN PRESERVED: the print methods still return `invisible(x)` in both option states.
    Build the Advice/es_advice fixtures the same way Phase 07 tests do (reuse helper builders if present). Do NOT hardcode a test count anywhere.

    Then run the FULL suite as the gate: the existing suite must stay green with the footer patch applied (CRAN-05: valid-input behavior unchanged; default path byte-identical).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/datascience/eventstudy && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test_advisor_pro_footer.R")' && Rscript -e 'devtools::test()'</automated>
    <fails_when>The new footer test file fails, OR any previously-green test now fails — testthat prints FAIL/ERROR summary and the process exits non-zero.</fails_when>
  </verify>
  <acceptance_criteria>
    - test_advisor_pro_footer.R asserts: default-silent, opt-in-prints-once (both print methods), no-network, invisible-return-preserved
    - Full `devtools::test()` suite is green (no new failures introduced by the footer patch)
    - No hardcoded test count in the test or its assertions
  </acceptance_criteria>
  <done>The footer behavior is locked by a regression test and the entire existing test suite stays green with the patch applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package → user terminal | The opt-in footer writes to the user's stdout; must be silent unless the user opts in |
| package → network | The waitlist must be a static URL only — the package must never open a connection on the user's behalf |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | .advisor_pro_footer / print methods | medium | mitigate | Footer emits a static constant string only; no env/telemetry read; default OFF, opt-in via option — verified by no-network + default-silent tests (Task 3) |
| T-08-02 | Tampering | SKILL reference files | low | mitigate | function-map cites only real NAMESPACE exports; grounding check (Task 1 verify) rejects invented symbols so the skill cannot instruct users to call non-existent code |
| T-08-03 | Denial of Service | print methods | low | accept | Footer is a bounded constant-length cat(); no loop/allocation risk |

No package-manager installs occur in this plan (no npm/pip/cargo, no new R Imports) — package-legitimacy gate not applicable.
</threat_model>

<verification>
- `.claude/skills/es-advisor/SKILL.md` + `reference/*.md` exist; function-map cites only real exports (Task 1 verifies)
- Footer: silent by default, prints static URL when option TRUE, no network, only in print methods (Task 2 + Task 3 verify)
- `?advisor_pro` topic and README waitlist section present
- Full `devtools::test()` suite green (Task 3)
</verification>

<success_criteria>
- SKILL-01/02/03: Agent Skill drives the full loop with only exported functions and documents the no-key offline degrade path
- BIZ-01: docs advertise the future Advisor Pro paid tier (?advisor_pro topic + README section)
- BIZ-02: passive waitlist — static URL, opt-in default-off footer, zero unsolicited network/telemetry
</success_criteria>

<output>
Create `.planning/phases/08-agent-skill-waitlist-green-check-gate/08-01-SUMMARY.md` when done
</output>
