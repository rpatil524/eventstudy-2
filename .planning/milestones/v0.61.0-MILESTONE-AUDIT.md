---
milestone: v0.61.0
milestone_name: Advisor Vignette + Dieselgate Walkthrough
audited: 2026-09-04T12:20:00Z
status: passed
scores:
  requirements: 16/16   # DOCS-02 legitimately N/A (no _pkgdown.yml)
  phases: 2/2
  integration: 1/1
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
nyquist:
  overall: not_applicable  # documentation milestone; validate-phase capability inactive
---

# Milestone v0.61.0 — Advisor Vignette + Dieselgate Walkthrough — Audit Report

**Audited:** 2026-09-04
**Status:** PASSED
**Definition of done:** Ship a CRAN vignette that explains the AI advisor's idea and
mechanics, anchored by a real Volkswagen dieselgate worked example, driven by a bundled
dataset that runs a valid end-to-end study offline, with aligned docs and a CRAN-clean
0.61.0 release.

This was a **VERIFICATION-only** pass — no package code was changed. Every requirement was
checked against the actually-shipped artifacts with `file:line` evidence, empirical results
were reproduced by running the pipeline and advisor layer, and the offline build + `R CMD
check --as-cran` + full test suite were executed.

---

## Scope

| Phase | Name | Verification | Score |
|-------|------|--------------|-------|
| 9 | Bundled Dieselgate Dataset + Provenance | passed | DATA-01..04 |
| 10 | Advisor Vignette + Offline-Safe Build + Docs + Release | passed | VIG/BUILD/DOCS/REL |

Milestone commits all present (`git log --oneline`):

- `0365753` feat(data): bundle reproducible VW dieselgate dataset (Phase 9, DATA-01..04)
- `c2c1783` docs(vignette): add AI advisor vignette with dieselgate walkthrough (Phase 10)
- `4852d27` chore(release): bump to 0.61.0 + NEWS + docs alignment
- `a6af7d6` docs(v0.61.0): mark Phase 10 + all requirements complete

---

## Requirements Coverage (per-requirement verdict)

### Dataset (DATA)

| ID | Verdict | Evidence |
|----|---------|----------|
| DATA-01 | **PASS** | `data-raw/dieselgate.R:1-91` — reproducible fetch via the package's own `download_stock_data()` (`:32`, `:43`), with a full provenance header recording source (Yahoo Finance), tickers (VOW.DE / ^GDAXI, `:9-10`), date range (2014-08-01→2015-11-30, `:14`), event date (2015-09-18, `:11`), and access date (2026-09-04, `:16`). Records source/tickers/date-range/access-date as comments exactly as required. |
| DATA-02 | **PASS** | `data/dieselgate.rda` present (4.3 KB, bzip2). Loads with no network: `data(dieselgate)` under `EVENTSTUDY_NO_NETWORK=1` returned a 4-element list (`firm`, `index`, `request`, `meta`), firm=336 rows, index=336 rows spanning the estimation + [-10,+10] event window. |
| DATA-03 | **PASS** | `R/data-dieselgate.R:1-62` (roxygen source) → `man/dieselgate.Rd:1-71`. Documents all columns of `firm`/`index`/`request`/`meta` (`Rd:9-27`), firm/index identities (`Rd:56-57`), event date 2015-09-18 (`Rd:46`), window layout (250-day estimation ending 11 days before event, [-10,+10] event window, `Rd:47-49`), and a provenance/licensing `\source` note ("illustrative sample … academic / demonstration use only", `Rd:29-34`). |
| DATA-04 | **PASS** | Reproduced empirically — see below. `EventStudyTask$new(firm,index,request)` → `run_event_study()` yields a non-degenerate, interpretable market-model study (beta≈1.086, R²≈0.7037, CAR[-10,+10]≈-0.355). No all-NA, no zero-variance collapse. |

### Vignette (VIG)

| ID | Verdict | Evidence |
|----|---------|----------|
| VIG-01 | **PASS** | `vignettes/ai-advisor.Rmd:23-67` — "Why the advisor exists" + "How it works: two layers" narrate the deterministic-offline-grounding vs. LLM-interpretation split and state the grounding invariant verbatim (`:34-37` "never fabricates a number… provably tied to a diagnostic the package actually computed") plus the runtime guard (`:57-63`). |
| VIG-02 | **PASS** | `:81-115` — live `load-data`, `run-pipeline` (`run_event_study()` wrapping prepare→fit→calculate), and `show-ar-car` chunks display AR near the event and the full-window CAR from the bundled data. Confirmed to run live (all `eval=TRUE`). |
| VIG-03 | **PASS** | `:133-170` — live `diagnostics` (`es_diagnostics()`), `recommend-stat` (`recommend_stat()`), `flag-robustness` (`flag_robustness()`) chunks. Reproduced with no API key — all three return real output (see empirical section). Setup chunk sets `EVENTSTUDY_NO_NETWORK=1` (`:20`). |
| VIG-04 | **PASS** | `:184-190` `llm-call` chunk is `eval = FALSE`; the captured `print(advice)` output is a plain non-executable fenced block wrapped in `BEGIN/END static captured LLM example (NOT executed)` HTML comments (`:200-236`); a dedicated "Why this block is not evaluated at build time" section explains the offline/no-API-key rationale (`:238-246`). |
| VIG-05 | **PASS** | Clear structural separation: "## What runs offline (deterministic)" (`:123`, labelled "evaluates live, no API key/network") vs. "## What the LLM adds (shown static)" (`:175`). The static block is visually fenced and captioned; the reader can tell which output is live-deterministic and which is captured-LLM. |

### Offline-safe build (BUILD)

| ID | Verdict | Evidence |
|----|---------|----------|
| BUILD-01 | **PASS** | Chunk audit (`grep '```{r'`): only `llm-call` carries `eval = FALSE`; the six deterministic chunks evaluate live. Static LLM output is precomputed prose, not a live call. |
| BUILD-02 | **PASS** | `env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY EVENTSTUDY_NO_NETWORK=1 R CMD build .` → `building 'EventStudy_0.61.0.tar.gz'`, "creating vignettes ... OK". Vignette also knits standalone under the same guards (`knitr::knit` → `KNIT_OK`). Built with no network, no API key, and without the optional AI Suggests being exercised. |

### Docs alignment (DOCS)

| ID | Verdict | Evidence |
|----|---------|----------|
| DOCS-01 | **PASS** | `README.md:73` names the "The Grounded AI Advisor" vignette (`vignette("ai-advisor")`) as the entry point and references `data(dieselgate)` (VW vs. DAX around the 2015 EPA disclosure); vignette index table lists `ai-advisor` (`README.md:383`). |
| DOCS-02 | **N/A (legitimately skipped)** | No `_pkgdown.yml` exists in the repo (confirmed absent). Matches the traceability table's "N/A (no _pkgdown.yml)". Nothing to configure. |
| DOCS-03 | **PASS** | `NEWS.md:1` `# EventStudy 0.61.0` heading; `:8-14` documents the `dieselgate` dataset, `:16-24` documents the "The Grounded AI Advisor" vignette and its offline build. |

### Release quality (REL)

| ID | Verdict | Evidence |
|----|---------|----------|
| REL-01 | **PASS** | `DESCRIPTION`: `Version: 0.61.0`, `VignetteBuilder: knitr`; `knitr` + `rmarkdown` present in Suggests (also `httr2 (>= 1.0.0)`, `jsonlite` for the optional LLM layer). |
| REL-02 | **PASS** | `R CMD check --as-cran` → **Status: 1 NOTE, 0 WARNING, 0 ERROR** (the pre-existing `median`/`tail` undefined-globals NOTE). No new NOTE/WARNING vs baseline; the old non-ASCII WARNING is gone. Suite green (0 failures). See details below. |

**Coverage:** 16/16 requirements resolved (15 PASS + 1 legitimate N/A). Unsatisfied: 0. Partial: 0.

---

## Empirical Verification (reproduced)

Ran the pipeline and both advisor layers via `devtools::load_all()` of the shipped source
tree, under `env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY EVENTSTUDY_NO_NETWORK=1` (no API key,
no network):

| Quantity | Expected (prompt) | Reproduced | Match |
|----------|-------------------|------------|-------|
| Market-model beta | ≈ 1.09 | **1.0862** | ✓ |
| Estimation R² | ≈ 0.70 | **0.7037** | ✓ |
| CAR[-10,+10] | ≈ -0.355 | **-0.355** | ✓ |
| AR day +1 | (crash) | **-0.174** (~-17%) | ✓ |
| AR day +2 | (crash) | **-0.127** (~-13%) | ✓ |
| Shapiro-Wilk p (est. window) | (non-normal) | **0.0042** | ✓ |
| Events (n) | single | **1** | ✓ |

Advisor layer, no API key:
- `es_diagnostics(task)` → `es_diagnostics` object printing R²=0.7037, Shapiro-Wilk p=0.0042,
  DW=2.14, 1 valid event. Real output.
- `recommend_stat(diag)` → 1 rule matched: `KB-NONNORM-NONPAR` (Brown & Warner 1985) —
  recommends non-parametric alternatives on the non-normal residuals. Deterministic, offline.
- `flag_robustness(diag)` → 1 rule matched: `KB-SMALL-N` — single-event small-sample caveat.
  Deterministic, offline.

All numbers cited in the vignette's static LLM block (beta≈1.09, R²≈0.70, Shapiro p, ~-35% CAR,
AR ~-17%/-12%) are grounded in the live deterministic output above — no fabricated figures.

---

## Offline Build Result

- `R CMD build .` (API keys unset, `EVENTSTUDY_NO_NETWORK=1`): **SUCCESS** —
  `EventStudy_0.61.0.tar.gz` built, "creating vignettes ... OK".
- Standalone `knitr::knit("vignettes/ai-advisor.Rmd")` under the same guards: **KNIT_OK**.
- No network, API key, or optional AI Suggests were needed to build the vignette.

---

## R CMD check --as-cran (NOTE/WARNING counts vs baseline)

Command: `env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY EVENTSTUDY_NO_NETWORK=1
_R_CHECK_FORCE_SUGGESTS_=false _R_CHECK_CRAN_INCOMING_REMOTE_=false R CMD check --as-cran
--no-manual EventStudy_0.61.0.tar.gz`

**Status: 1 NOTE — 0 WARNING — 0 ERROR.**

| Item | Baseline (v0.60.0) | v0.61.0 | Delta |
|------|--------------------|---------|-------|
| Undefined-globals NOTE (`median`, `tail`) | present | present | unchanged |
| CRAN-archived-2024 incoming-feasibility NOTE | present (remote-only) | not triggered locally (remote check disabled) | unchanged in kind |
| Non-ASCII WARNING | fixed pre-v0.61.0 | **absent** ("non-ASCII characters ... OK") | still fixed |
| **New NOTEs/WARNINGs** | — | **0** | ✓ meets REL-02 |

Notes:
- The single local NOTE is the long-standing `median`/`tail` undefined-globals NOTE — part of
  the documented baseline, not a milestone regression.
- The CRAN-archived incoming-feasibility NOTE only fires under a remote incoming check
  (`_R_CHECK_CRAN_INCOMING_REMOTE_=true`, i.e. on CRAN's own servers); it was disabled here to
  keep the run offline. It is a known baseline item, unchanged by this milestone.
- Zero new NOTEs/WARNINGs relative to baseline — REL-02 satisfied.

**Environmental caveat (not a defect):** a first `--as-cran` run ERRORed with "Packages
suggested but not available: rugarch, rmgarch, did, DIDmultiplegt, didimputation, DT" —
these optional Suggests are simply not installed in this offline sandbox. Re-running with
`_R_CHECK_FORCE_SUGGESTS_=false` (the standard way to check without optional Suggests) cleared
it to the 1-NOTE result above. This is a sandbox limitation, not a package problem; CRAN's
machines have these packages.

---

## Test Suite

- Full suite via `testthat::test_dir("tests/testthat")` under the offline guards:
  **1855 PASS / 0 FAIL / 1 WARN / 57 SKIP.**
- The suite also ran clean inside `R CMD check` (`Running 'testthat.R' [61s] OK`).
- The single WARN is an **expected** edge-case assertion: `test_edge_cases.R:817`
  (`cross_sectional .extract_cars returns NA for all-NA abnormal returns`) exercises a
  rank-deficient design matrix and the warning is the behavior under test — not a regression.
- Pass count (1855) is lower than the ~1921 cited and the 1913 v0.60.0 baseline **only because
  more tests skip in this sandbox** (57 skips vs 31 baseline): the missing Suggests
  (rugarch/did/etc.) and `EVENTSTUDY_NO_NETWORK`/`On CRAN` guards skip the corresponding blocks.
  The load-bearing fact is **0 failures** — the suite stays green. On a machine with all
  Suggests installed the skips convert back to passes.

---

## Cross-Phase Integration (E2E flow)

Verdict: **1/1 flow VERIFIED.** The dataset (Phase 9) drives the vignette walkthrough
(Phase 10) end-to-end: `data(dieselgate)` → `EventStudyTask$new()` → `run_event_study()`
(prepare→fit→calculate) → `es_diagnostics()` → `recommend_stat()`/`flag_robustness()` →
static `es_advise()` interpretation. Reproduced live; every seam connects with real,
non-degenerate output. The v0.60.0 advisor exports the vignette depends on all resolve
(`es_diagnostics`, `recommend_stat`, `flag_robustness`, `es_advise`, `provider`).

---

## Tech Debt / Gaps / Follow-ups

- **None blocking.** No requirement is FAIL or PARTIAL.
- Minor, pre-existing (out of scope for this doc milestone): the `median`/`tail`
  undefined-globals NOTE could be cleared by adding `importFrom("stats","median")` /
  `importFrom("utils","tail")` to NAMESPACE — but it is baseline and REL-02 explicitly
  measures *new* NOTEs (0). Not introduced by v0.61.0.
- Deferred by design (tracked, not gaps): Advisor Pro RAG corpus, MCP surface, additional
  vignettes (model-selection, panel-DiD) — per REQUIREMENTS.md "Future Requirements".

---

## Verdict

**PASSED.** All 16 requirements resolve (15 PASS + DOCS-02 legitimately N/A), both phases
verified, the dataset drives a valid non-degenerate end-to-end study with the exact expected
numbers (beta≈1.09, R²≈0.70, CAR≈-0.355), the vignette cleanly separates a live deterministic
layer from a static/labelled LLM layer, the package builds and its vignette compiles fully
offline with no API key or optional AI Suggests, `R CMD check --as-cran` shows 0 new
NOTEs/WARNINGs vs baseline (1 baseline NOTE, 0 WARNING, 0 ERROR; the old non-ASCII WARNING
stays fixed), and the test suite is green (0 failures). The milestone achieved its definition
of done. Ready to complete and archive.
