---
phase: 03-pipeline-and-external-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/panel_event_study.R
  - R/models.R
  - R/models_time_varying.R
  - R/synthetic_control.R
  - tests/testthat/test_panel.R
  - tests/testthat/test_models_time_varying.R
  - tests/testthat/test_synthetic_control.R
autonomous: true
requirements: [EXTERNAL-01, EXTERNAL-02, EXTERNAL-03, EXTERNAL-04]
tags: [external-packages, panel-did, garch, dcc-garch, synthetic-control, tryCatch, requireNamespace, testthat, mocking]

estimate:
  tokens: 68000
  raw_tokens: 34000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "estimate_panel_event_study() with method='dechaisemartin_dhaultfoeuille' when DIDmultiplegt is absent OR its call errors/segfault-wrapped emits a clear warning and returns a task whose results are NULL — the R session never dies (SC4, EXTERNAL-02)."
    - "did (callaway_santanna) and didimputation (borusyak_jaravel_spiess) absent -> warning() naming the lost capability + NULL results (was stop()); the actual att_gt/did_imputation/did_multiplegt call sites are tryCatch-wrapped so an external runtime failure degrades to warning+NULL, not a crash (EXTERNAL-01/03)."
    - "sandwich absent in .compute_se() emits a warning() (upgraded from message()) naming cluster-robust SEs as the lost capability + OLS fallback; the vcovCL call is tryCatch-wrapped (EXTERNAL-03)."
    - "GARCHModel (rugarch) and DCCGARCHModel (rmgarch): a failure inside calculate_statistics after is_fitted was set TRUE resets is_fitted=FALSE + emits a named warning + returns NA abnormal returns; the Phase 2 pre-call degenerate guards are UNTOUCHED (SC5, EXTERNAL-04)."
    - "Synthetic control: quadprog::solve.QP on a singular/collinear donor pool (and max() on an empty donor pool) degrade gracefully (warning + NA/optim fallback) instead of crashing (EXTERNAL-04)."
  artifacts:
    - "R/panel_event_study.R: 3 stop()->warning()+NULL (did 409, DIDmultiplegt 456, didimputation 549); message()->warning() (171); tryCatch around att_gt (415), did_multiplegt (462), did_imputation (555), vcovCL (168); DIDmultiplegt opt-in probe + skip option"
    - "R/models.R: tryCatch around GARCHModel private$calculate_statistics(); is_fitted reset on failure"
    - "R/models_time_varying.R: tryCatch around DCCGARCHModel private$calculate_statistics(); is_fitted reset on failure"
    - "R/synthetic_control.R: tryCatch around quadprog::solve.QP + empty-donor-pool guard on max(theta)"
    - "tests/testthat/test_panel.R, test_models_time_varying.R, test_synthetic_control.R (expanded) — absence/failure degrade paths via local_mocked_bindings + happy paths via skip_if_not_installed"
  key_links:
    - "estimate_panel_* helpers mutate task$results and the outer estimate_panel_event_study() returns task regardless — an early return(invisible(NULL)) leaves task$results NULL; callers detect failure via is.null(task$results) (Pitfall 2)"
    - "tryCatch around calculate_statistics MUST reset private$.is_fitted<-FALSE in the error handler or the model reports is_fitted=TRUE with no statistics (Pitfall 4)"
    - "requireNamespace mocks in tests MUST be scoped .package='EventStudy' or the real function still runs (Pitfall 3)"
    - "DIDmultiplegt subprocess probe MUST stay gated behind requireNamespace('callr') AND getOption('eventstudy.probe_didmultiplegt', FALSE) — callr is NOT a dependency (no DESCRIPTION change)"
---

<objective>
Defensively wrap every external-package-bound area so absence or runtime failure of an optional package degrades to a named warning + NULL/NA rather than a `stop()` or a session-killing crash. Covers the panel-DiD estimators (did / DIDmultiplegt / didimputation), the deferred rugarch/rmgarch FAILURE wrapping from Phase 2, and synthetic-control numerical stability.

Purpose: Satisfy success criteria SC4 and SC5 (and EXTERNAL-01/02/03/04) — the uniform missing-package policy (warning + NULL), DIDmultiplegt segfault isolation, GARCH/DCC failure handling, and SC numeric guards.

Output: Hardened `R/panel_event_study.R`, `R/models.R`, `R/models_time_varying.R`, `R/synthetic_control.R`, plus absence/failure regression tests. No new hard dependency (callr stays optional via requireNamespace); Phase 2 pre-call guards untouched; valid-input behavior byte-identical.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-pipeline-and-external-hardening/03-CONTEXT.md
@.planning/phases/03-pipeline-and-external-hardening/03-RESEARCH.md
@.claude/CLAUDE.md

# Targets — Read only the research-named ranges, do not re-read whole files:
# R/panel_event_study.R: .compute_se (166-175), did (405-449), DIDmultiplegt (452-524), didimputation (549-582)
# R/models.R: GARCHModel rugarch block (1001-1083) — Phase 2 guards 1001-1042 are OFF-LIMITS
# R/models_time_varying.R: DCCGARCHModel rmgarch block (256-357, calculate_statistics 385-395)
# R/synthetic_control.R: .solve_sc_optim (204-231), .solve_sc_quadprog (184-199), placebo (266,301)
@R/panel_event_study.R
</context>

<artifacts_this_phase_produces>
- `R/panel_event_study.R`: 3 `stop()` -> `warning(call.=FALSE)+return(invisible(NULL))`; `message()` -> `warning()` at 171; tryCatch wrapping att_gt (415), did_multiplegt (462), did_imputation (555), vcovCL (168); new opt-in `options(eventstudy.probe_didmultiplegt)` gate for the callr subprocess probe and `options(eventstudy.skip_didmultiplegt)` opt-out.
- `R/models.R`: tryCatch around GARCHModel `private$calculate_statistics(data_tbl)`; error handler sets `private$.is_fitted <- FALSE` + named warning.
- `R/models_time_varying.R`: same treatment for DCCGARCHModel `private$calculate_statistics(data_tbl)`.
- `R/synthetic_control.R`: tryCatch around `quadprog::solve.QP` (fallback to optim / NA + warning); empty-donor-pool guard before `max(theta)`.
- Expanded `test_panel.R`, `test_models_time_varying.R`, `test_synthetic_control.R`.
- No DESCRIPTION change. `callr` stays optional (requireNamespace gate); no new hard dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Panel-DiD absence + failure wrapping, one estimator end-to-end then the other three (EXTERNAL-01/02/03)</name>
  <files>R/panel_event_study.R, tests/testthat/test_panel.R</files>
  <read_first>
    - R/panel_event_study.R lines 160-175 (.compute_se), 405-449 (callaway_santanna), 452-524 (dechaisemartin), 549-582 (borusyak). The stop() sites (409, 456, 549), the call sites (att_gt 415, did_multiplegt 462, did_imputation 555, vcovCL 168), and message() (171) are in context.
    - 03-RESEARCH.md sections 4a-4d + Patterns 2 & 3 (warning+NULL for missing package; tryCatch around external call) + Pitfall 2 (helpers mutate task$results; early NULL leaves results NULL) + Pitfall 3 (mock requireNamespace with .package="EventStudy") + Pitfall 5 (include conditionMessage(e) so API mismatch is visible).
  </read_first>
  <behavior>
    - did absent: estimate_panel_event_study(method="callaway_santanna") emits one warning naming 'did' + the lost capability, and returns a task with results NULL (no stop()).
    - DIDmultiplegt absent OR its call errors: method="dechaisemartin_dhaultfoeuille" emits one warning + returns task with results NULL; the session survives. With getOption("eventstudy.skip_didmultiplegt")=TRUE the call is skipped with a warning. The callr subprocess probe runs ONLY when both requireNamespace("callr") AND getOption("eventstudy.probe_didmultiplegt", FALSE) are TRUE; otherwise tryCatch-only.
    - didimputation absent OR its call errors: method="borusyak_jaravel_spiess" -> warning + results NULL.
    - sandwich absent in .compute_se(): warning() (not message()) naming cluster-robust SEs + OLS fallback; the vcovCL call itself is tryCatch-wrapped so a singular cluster structure warns + falls back rather than crashing.
    - With the packages present and a valid panel, existing estimator output is byte-identical (happy paths guarded by skip_if_not_installed).
  </behavior>
  <action>
    Start with ONE estimator end-to-end (callaway_santanna) to prove the wrap+test pattern, then replicate to the other two and .compute_se.

    (1) callaway_santanna (EXTERNAL-01): replace the stop() at 409-412 with `warning("Package 'did' is not installed - callaway_santanna estimator unavailable. Install with: install.packages('did')", call.=FALSE); return(invisible(NULL))`. Wrap the `did::att_gt(...)` + `did::aggte(...)` region (415-426) in tryCatch; on error `warning("did::att_gt failed: ", conditionMessage(e), " - returning NULL.", call.=FALSE); return(invisible(NULL))`. Leave the coef_tbl construction and `task$results <- ...` unchanged for the success path.

    (2) dechaisemartin (EXTERNAL-02): replace stop() at 456-459 with warning+return(invisible(NULL)) naming DIDmultiplegt + suggesting callaway_santanna/BJS alternatives. Add, before the call: an opt-out `if (isTRUE(getOption("eventstudy.skip_didmultiplegt"))) { warning(...); return(invisible(NULL)) }`; and an OPT-IN probe `if (requireNamespace("callr", quietly=TRUE) && isTRUE(getOption("eventstudy.probe_didmultiplegt", FALSE))) { ok <- tryCatch(callr::r(function() requireNamespace("DIDmultiplegt", quietly=TRUE), timeout=30), error=function(e) FALSE); if (!isTRUE(ok)) { warning("DIDmultiplegt failed to load in a subprocess probe - skipping call.", call.=FALSE); return(invisible(NULL)) } }`. Wrap the `DIDmultiplegt::did_multiplegt(...)` call (462-472) in tryCatch -> warning (with conditionMessage(e), per Pitfall 5) + return(invisible(NULL)). callr MUST stay behind requireNamespace (no DESCRIPTION change).

    (3) borusyak (EXTERNAL-03): replace stop() at 549-552 with warning+return(invisible(NULL)); wrap `didimputation::did_imputation(...)` (555-562) in tryCatch -> warning+NULL.

    (4) .compute_se (EXTERNAL-03): upgrade the `message(...)` at 171 to `warning(...)` naming cluster-robust SEs as the lost capability + OLS fallback. Wrap the `sandwich::vcovCL(...)` call (168) in tryCatch; on error warning + fall back to `summary(fit)$coefficients[, 2]`.

    Tests: for did / DIDmultiplegt / didimputation / sandwich, use `testthat::local_mocked_bindings(requireNamespace = function(pkg, ...) pkg != "<pkg>", .package = "EventStudy")` to simulate absence WITHOUT uninstalling, asserting expect_warning naming the pkg AND is.null(result$results) (per Pitfall 2, check task$results not the return value alone). Add a skip_if_not_installed happy-path test per estimator that asserts results is non-NULL and unchanged. Add a test that sets options(eventstudy.skip_didmultiplegt=TRUE) -> warning + NULL results. Reset options in on.exit / withr.

    Do NOT write any acceptance-criteria phrase verbatim into a code comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_panel.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>An absent estimator package still stop()s; a wrapped external call crashes instead of warning+NULL; task$results is non-NULL on a degrade path; sandwich-absent path emits message() instead of warning(); or the callr probe runs when its option is unset.</fails_when>
  </verify>
  <done>All four panel wrap points (att_gt, did_multiplegt, did_imputation, vcovCL) degrade to warning+NULL/OLS on absence or failure; opt-out/opt-in options work; happy paths unchanged; test_panel.R passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rugarch/rmgarch calculate_statistics failure wrapping — is_fitted reset (EXTERNAL-04)</name>
  <files>R/models.R, R/models_time_varying.R, tests/testthat/test_models_time_varying.R</files>
  <read_first>
    - R/models.R GARCHModel block 1057-1083 — the purrr::safely(ugarchfit) wrapper, the `if (converged) { ...; private$.is_fitted <- TRUE; private$calculate_statistics(data_tbl) }` at ~1073-1074. Phase 2 guards at 1001-1042 are OFF-LIMITS (do not touch).
    - R/models_time_varying.R DCCGARCHModel block 333-357 — same shape; calculate_statistics at ~347; rcov usage 385-395.
    - 03-RESEARCH.md section 5 + Pitfall 4 (tryCatch around calculate_statistics MUST reset is_fitted=FALSE or the model reports fitted with no statistics). OQ-4 recommendation: reset to FALSE + named warning.
  </read_first>
  <behavior>
    - GARCHModel: if `private$calculate_statistics(data_tbl)` throws after is_fitted was set TRUE, the model resets is_fitted=FALSE, emits one named warning ("GARCH ... statistics computation failed ..."), and abnormal_returns() returns all-NA.
    - DCCGARCHModel: identical behavior around its calculate_statistics call.
    - The existing pre-call degenerate guards (Phase 2) and the convergence-failure / fit-error branches are UNCHANGED — a normal successful GARCH/DCC fit produces byte-identical statistics.
  </behavior>
  <action>
    In GARCHModel (R/models.R), wrap ONLY the `private$calculate_statistics(data_tbl)` call inside the `if (converged)` branch in tryCatch: on error, `private$.is_fitted <- FALSE; warning("GARCH model statistics computation failed: ", conditionMessage(e), ". Returning NA abnormal returns.", call.=FALSE)`. Do NOT alter the `private$.is_fitted <- TRUE` assignment order beyond wrapping — leave lines 1001-1042 (Phase 2 guards) and the convergence/error branches exactly as-is.

    In DCCGARCHModel (R/models_time_varying.R), apply the identical tryCatch around its `private$calculate_statistics(data_tbl)` (~line 347) with a "DCC-GARCH ... statistics computation failed" named warning and is_fitted reset.

    Tests (test_models_time_varying.R): gated by skip_if_not_installed("rugarch") / ("rmgarch"). Force calculate_statistics to fail — prefer `local_mocked_bindings` on the rugarch/rmgarch accessor the private method calls (e.g. rugarch::sigma / rmgarch::rcov) to throw, then assert is_fitted becomes FALSE, exactly one named warning is emitted, and abnormal_returns() is all-NA. Also keep a happy-path assertion (valid fit -> is_fitted TRUE, statistics finite). If mocking the internal accessor is impractical, document the alternative in the SUMMARY and assert via a corrupted fitted-model object; do NOT weaken the is_fitted-reset assertion.

    Do NOT embed any acceptance-criteria phrase in a code comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_models_time_varying.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>A calculate_statistics failure leaves is_fitted=TRUE; no named warning is emitted; abnormal_returns() is not all-NA on failure; or a valid GARCH/DCC fit's statistics changed; or the Phase 2 pre-call guards were modified.</fails_when>
  </verify>
  <done>GARCH/DCC calculate_statistics failures reset is_fitted=FALSE + named warning + NA; Phase 2 guards untouched; happy path unchanged; test_models_time_varying.R passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthetic-control numerical guards — solve.QP + empty donor pool (EXTERNAL-04)</name>
  <files>R/synthetic_control.R, tests/testthat/test_synthetic_control.R</files>
  <read_first>
    - R/synthetic_control.R lines 184-199 (.solve_sc_quadprog, quadprog::solve.QP at 197, ridge at 188), 204-231 (.solve_sc_optim, .stable_softmax 210-214, optim 222 with convergence warning at 225), 266/301 (placebo MSPE, already epsilon-guarded).
    - 03-RESEARCH.md section 6 — softmax/MSPE/ridge are ALREADY correct (do not touch); the two real gaps are (a) no tryCatch around quadprog::solve.QP and (b) max(theta) on an empty donor pool (n=0) throwing "no non-missing arguments to max".
  </read_first>
  <behavior>
    - A singular/perfectly-collinear donor pool passed to .solve_sc_quadprog (Dmat singular despite the 1e-8 ridge) does not crash: solve.QP failure -> warning + fall back to the optim solver (or return NA weights + warning if optim also unavailable).
    - An empty donor pool (n=0) in .solve_sc_optim does not throw from max(theta): guard before max() -> warning + graceful NA return.
    - A valid donor pool produces byte-identical weights (softmax, MSPE, ridge paths are unchanged).
  </behavior>
  <action>
    In .solve_sc_quadprog, wrap `quadprog::solve.QP(Dmat, dvec, Amat, bvec, meq = meq)` (line 197) in tryCatch; on error `warning("Synthetic control: quadprog::solve.QP failed (singular/collinear donor pool): ", conditionMessage(e), " - falling back to optim.", call.=FALSE)` and return a sentinel (e.g. NULL) that the caller uses to route to .solve_sc_optim; if optim is also the active path and fails, return NA weights + warning. Keep the existing 1e-8 ridge and softmax exactly as-is.

    In .solve_sc_optim, add a guard before `max(theta)` / the optim call: if the donor count n == 0 (theta is numeric(0)), `warning("Synthetic control: empty donor pool - cannot estimate weights.", call.=FALSE)` and return NA weights of the appropriate length gracefully (do NOT let max(numeric(0)) throw). Do NOT modify .stable_softmax (already overflow-safe) or the MSPE epsilon guards (already correct).

    Tests (test_synthetic_control.R, gated by skip_if_not_installed("quadprog") where the QP path is exercised): (a) collinear donor pool -> expect_warning + no error + graceful weights/fallback; (b) empty donor pool -> expect_warning + no "max" crash; (c) a valid donor pool -> weights unchanged vs a pre-change baseline.

    Do NOT write any acceptance-criteria phrase verbatim into a code comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_synthetic_control.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>A singular donor pool crashes solve.QP; an empty donor pool throws from max(); the softmax/MSPE/ridge paths were altered; or valid-pool weights changed.</fails_when>
  </verify>
  <done>Singular donor pool -> warning + optim/NA fallback; empty donor pool -> warning + graceful NA; already-correct numeric paths untouched; test_synthetic_control.R passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional external package (did / DIDmultiplegt / didimputation / rugarch / rmgarch / sandwich / quadprog) -> EventStudy | An absent or runtime-failing/segfault-prone optional package crosses into the estimator call sites. This is the only boundary of interest; there is no untrusted-network/auth boundary in an internal statistical library. |
| user panel/donor data -> external estimator | Degenerate numeric input (singular clusters, collinear donors) reaches external solvers that may crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | DIDmultiplegt::did_multiplegt segfault (macOS arm64) | high | mitigate | tryCatch around the call + opt-out option(eventstudy.skip_didmultiplegt) + opt-in callr subprocess probe (requireNamespace-gated) so the session never dies (Task 1). |
| T-03-05 | Denial of Service | did / didimputation runtime failure or absence | medium | mitigate | stop()->warning()+NULL and tryCatch around att_gt/did_imputation (Task 1). |
| T-03-06 | Information Disclosure | sandwich absent -> silent non-robust SEs | low | mitigate | message()->warning() naming the lost robust-SE capability; vcovCL tryCatch-wrapped (Task 1). |
| T-03-07 | Denial of Service | rugarch/rmgarch calculate_statistics failure leaving is_fitted=TRUE | medium | mitigate | tryCatch resets is_fitted=FALSE + named warning + NA (Task 2). |
| T-03-08 | Denial of Service | quadprog::solve.QP / max() crash on singular/empty donor pool | medium | mitigate | tryCatch + empty-pool guard -> warning + optim/NA fallback (Task 3). |

No package-install (npm/pip/cargo) actions occur; `callr` stays optional via `requireNamespace()` with NO DESCRIPTION change, so the package-legitimacy gate does not apply. No network endpoints, authentication, sessions, or access-control surfaces are introduced.
</threat_model>

<verification>
Per-area quick suites:
```
Rscript -e 'devtools::load_all("."); for (f in c("tests/testthat/test_panel.R","tests/testthat/test_models_time_varying.R","tests/testthat/test_synthetic_control.R")) { df <- as.data.frame(testthat::test_file(f, reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error)) }'
```
Whole-suite regression (existing ~1200 tests stay green; no new R CMD check NOTE/WARNING):
```
Rscript -e 'df <- as.data.frame(devtools::test(reporter="silent")); stopifnot(sum(df$failed)==0, sum(df$error)==0)'
```
</verification>

<success_criteria>
- SC4: DIDmultiplegt absent/segfault-prone -> clear warning + task$results NULL; R session never dies.
- SC5: any optional package (sandwich, rugarch, did, didimputation) absent -> named warning describing the lost capability, not a silent non-robust fallback.
- EXTERNAL-04: GARCH/DCC calculate_statistics failure -> is_fitted=FALSE + named warning + NA; SC solve.QP/empty-pool degrade gracefully.
- Phase 2 pre-call degenerate guards untouched; no new hard dependency; valid-input behavior byte-identical; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-pipeline-and-external-hardening/03-02-SUMMARY.md` when done.
</output>
