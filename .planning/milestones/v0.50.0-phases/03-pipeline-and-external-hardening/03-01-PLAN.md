---
phase: 03-pipeline-and-external-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/prepare_event_study.R
  - R/export.R
  - R/cross_sectional.R
  - tests/testthat/test_prepare.R
  - tests/testthat/test_export.R
  - tests/testthat/test_cross_sectional.R
autonomous: true
requirements: [PIPELINE-01, PIPELINE-02, PIPELINE-03]
tags: [pipeline, prepare, export, cross_sectional, degenerate-input, contract, R6, testthat]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A non-existent event date through prepare_event_study() in lenient mode emits exactly one warning NAMING the offending event (event_id + firm_symbol) and does not crash; the event's data_tbl carries all-zero windows so the model layer degrades cleanly (SC1, PIPELINE-01)."
    - "The same non-existent event date in strict mode raises stop() whose message names the component, event_id, and firm_symbol (SC1, PIPELINE-01)."
    - "export_results() and tidy() on a task with NA abnormal returns produce valid output (NAs rendered as NA/blank), never crashing from an if() on NA; CAR columns use cumsum(coalesce(abnormal_returns,0)) while the AR column preserves raw NA (SC2, PIPELINE-02)."
    - "cross_sectional_regression() on a collinear/singular firm-characteristic matrix returns NA coefficients + one warning rather than crashing on a singular-matrix error (SC3, PIPELINE-03)."
    - "sandwich absent in cross_sectional_regression(robust=TRUE) emits a warning() (upgraded from message()) NAMING the lost capability (robust SEs) and the OLS fallback (EXTERNAL-03 pipeline half)."
  artifacts:
    - "R/prepare_event_study.R with .append_windows() routing missing-date/empty-window through .handle_degenerate()"
    - "R/export.R with coalesce-guarded CAR cumsum in export + tidy-car paths"
    - "R/cross_sectional.R with tryCatch-wrapped lm()+vcovHC and rank-deficiency guard; message()->warning() at line 75"
    - "tests/testthat/test_prepare.R (new) — missing-date + empty-window, both modes"
    - "tests/testthat/test_export.R + test_cross_sectional.R (expanded) — NA-safety + singular-design regressions"
  key_links:
    - "prepare_event_study() must thread degenerate mode + event_id + firm_symbol from task$data_tbl into .append_windows() (map2 -> explicit row-indexed map)"
    - ".handle_degenerate() from R/contract.R is the SINGLE dispatch point for event-identifiable pipeline degeneracies (missing date, empty window)"
    - "cross_sectional coalesce/NA propagation must reach coef_tbl so NA coefficients surface in the returned object, not Inf/NaN"
---

<objective>
Harden the pipeline-side hardening targets: `prepare_event_study()` missing-date / empty-window handling, `export_results()`/`tidy()` NA-safety, and `cross_sectional_regression()` singular/collinear designs. All event-identifiable degeneracies route through the Phase 1 contract (`.handle_degenerate()`); package-availability issues become plain `warning()`s.

Purpose: Satisfy success criteria SC1, SC2, SC3 (and the pipeline half of the uniform optional-package policy) so the prepare/export/cross-sectional paths degrade predictably instead of crashing or returning silently-empty/wrong results.

Output: Hardened `R/prepare_event_study.R`, `R/export.R`, `R/cross_sectional.R`, plus new/expanded regression tests. Valid-input behavior byte-identical; existing suite stays green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-pipeline-and-external-hardening/03-CONTEXT.md
@.planning/phases/03-pipeline-and-external-hardening/03-RESEARCH.md
@.claude/CLAUDE.md

# Contract helpers to REUSE (do not reimplement):
@R/contract.R
# Phase 1 threading pattern to REPLICATE (explicit row-indexed map, NOT pmap-inside-mutate):
@.planning/phases/01-contract-foundation/01-01-SUMMARY.md
# Targets to modify:
@R/prepare_event_study.R
@R/cross_sectional.R
# export.R is large — Read the ranges the research names (57-131, 261-425) before editing; do not re-read.
</context>

<artifacts_this_phase_produces>
- `.append_windows()` gains signature `(data_tbl, request, mode = "lenient", event_id = NULL, firm_symbol = NULL)` and routes the missing-date branch through `.handle_degenerate()` (@noRd, no new export).
- `prepare_event_study()` threads `mode`/`event_id`/`firm_symbol` via an explicit row-indexed `purrr::map(seq_len(nrow(task$data_tbl)), ...)` (matching Phase 1's NSE-safe pattern) instead of `purrr::map2`.
- `R/export.R` CAR columns use `cumsum(dplyr::coalesce(abnormal_returns, 0))` in the `.build_export_tables` CAR path and `.tidy_car`; the AR column keeps raw NA.
- `R/cross_sectional.R`: tryCatch around `lm()` + `sandwich::vcovHC`; rank-deficiency check; NA-coefficient fallback; `message()` -> `warning()` at line 75.
- New `tests/testthat/test_prepare.R`; expanded `test_export.R` and `test_cross_sectional.R`.
- No new package dependency; no DESCRIPTION change.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end missing-date degradation through prepare_event_study() — one path, both modes (PIPELINE-01)</name>
  <files>R/prepare_event_study.R, tests/testthat/test_prepare.R</files>
  <read_first>
    - R/prepare_event_study.R lines 12-62 (prepare_event_study) and 92-122 (.append_windows) — already in context.
    - R/contract.R `.handle_degenerate()` (lines 74-97) and `.resolve_degenerate_mode()` (lines 51-61) — the reuse target; note the message format `component [event_id=..] [firm=..]: condition`.
    - .planning/phases/01-contract-foundation/01-01-SUMMARY.md — REPLICATE the "explicit row-indexed purrr::map(seq_len(nrow(...)), function(i) ...)" threading pattern; do NOT use pmap-inside-dplyr::mutate (NSE ambiguity, per D-01 Phase 1 decision).
  </read_first>
  <behavior>
    - Lenient (default) + non-existent event date: prepare_event_study() completes, emits exactly ONE warning whose text contains the event_id AND firm_symbol of the offending row (via .handle_degenerate). The offending row's data_tbl has relative_index = NA_real_, event_window = 0L, estimation_window = 0L for all rows (OQ-1 recommendation) so a downstream filter(estimation_window == 1) yields zero rows without crashing.
    - Strict + non-existent event date: prepare_event_study() raises stop() whose message contains "prepare_event_study", the event_id, and the firm_symbol.
    - A VALID event date is unaffected: the produced event_window/estimation_window columns are identical to pre-change output (byte-identical windows) — assert against a fixture built from create-mock data with a real event date.
  </behavior>
  <action>
    Thread the degenerate contract through the window-building step, wiring ONE end-to-end path (prepare -> .append_windows -> .handle_degenerate) before touching export or cross_sectional.

    (1) In `prepare_event_study()`, immediately after the ParameterSet validity check, resolve the mode ONCE: `mode <- .resolve_degenerate_mode(parameter_set$degenerate_handling)`.

    (2) Replace the `purrr::map2(.x=data, .y=request, .f=.append_windows)` block (lines 29-32) with an explicit row-indexed map that captures `mode` as a closure variable and pulls `event_id`/`firm_symbol` from the outer `task$data_tbl` columns — REPLICATE the Phase 1 pattern from 01-01-SUMMARY: build `task$data_tbl$data <- purrr::map(seq_len(nrow(task$data_tbl)), function(i) .append_windows(task$data_tbl$data[[i]], task$data_tbl$request[[i]], mode = mode, event_id = task$data_tbl$event_id[[i]], firm_symbol = task$data_tbl$firm_symbol[[i]]))`. Do NOT use pmap-inside-mutate.

    (3) Change `.append_windows` signature to `(data_tbl, request, mode = "lenient", event_id = NULL, firm_symbol = NULL)`.

    (4) Replace the `stop(...)` block at lines 109-113 with a call to `.handle_degenerate(mode = mode, condition = paste0("event date '", request$event_date, "' not found in trading data (or found multiple times)"), component = "prepare_event_study", event_id = event_id, firm_symbol = firm_symbol)` (do NOT pass private_env — this is a free function, per OQ-1). Then, when execution continues in lenient mode (strict already stopped), `return(dplyr::mutate(data_tbl, relative_index = NA_real_, event_window = 0L, estimation_window = 0L))` — the all-zero windows per OQ-1 recommendation, so the model layer's n_valid guard fires cleanly.

    Write test_prepare.R asserting the three behaviors above using the existing mock-data helper (helper-mock-data.R). Use `withCallingHandlers` to count warnings (assert length == 1 and grepl the keys) for lenient; `tryCatch(..., error=)` + grepl for strict. Reuse the Phase 2 verify idiom (mode field on ParameterSet) shown in prior_verify_commands.

    Do NOT put any literal acceptance-criteria phrase into a code comment. Comments explain WHY (the all-zero-window rationale), not restate the assertion.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_prepare.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>Missing-date lenient path crashes, emits != 1 warning, or the warning omits event_id/firm_symbol; strict path does not stop() or its message omits the keys; a valid event date produces different window columns than before.</fails_when>
  </verify>
  <done>Missing-date and empty-window handling honors the degenerate mode end-to-end; test_prepare.R passes; valid-date windowing unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: export_results()/tidy() NA-safety — coalesce-guarded CAR + regression tests (PIPELINE-02)</name>
  <files>R/export.R, tests/testthat/test_export.R</files>
  <read_first>
    - R/export.R lines 57-131 (`.build_export_tables`, including the CAR `cumsum(abnormal_returns)` at ~line 88) and 261-425 (`.tidy_ar`, `.tidy_car`, `.tidy_aar`, `.compute_pval`). The research VERIFIED that no crash-producing NA-on-if exists today; the change is a defensive cumsum upgrade + regression tests (OQ-2 option (b): do BOTH).
    - 03-RESEARCH.md section 2 (PIPELINE-02) — the enumerated audit of every risky if(); confirm each branch it lists as SAFE stays untouched.
  </read_first>
  <behavior>
    - export_results() / tidy() on a task whose abnormal_returns contain NA (e.g., an event that fitted is_fitted=FALSE) returns a valid table with NAs rendered as NA (not an error, not silently dropped rows).
    - CAR columns (the cumulative-sum path in `.build_export_tables` ~line 88 and `.tidy_car`) use `cumsum(dplyr::coalesce(abnormal_returns, 0))` so a mid-window NA does not blank out the entire tail of the CAR series; the AR column preserves raw NA unchanged.
    - Valid (all-finite) abnormal returns produce byte-identical export/tidy output to before (coalesce is a no-op on finite input).
  </behavior>
  <action>
    In `.build_export_tables` replace the CAR `cumsum(abnormal_returns)` with `cumsum(dplyr::coalesce(abnormal_returns, 0))` (CAR path only — leave the raw AR column as `abnormal_returns`). Apply the same coalesce to the cumulative-sum in `.tidy_car`. Do NOT alter any of the if() branches the research audited as SAFE (lines 66, 80, 92, 93, 105, and the has_sigma/has_df guards in .tidy_ar/.tidy_car/.tidy_aar) — they are already NA-safe via short-circuit `&&`.

    Add regression tests to test_export.R that: build a task, force one row's model to is_fitted=FALSE (or inject NA abnormal_returns via the degenerate factories used in Phase 2), then assert export_results() and tidy() each (a) do not error, (b) return NA (not 0, not dropped) in the AR column for the NA rows, and (c) return a CAR series that is finite up to the first real NA-free segment and does not become all-NA from a single interior NA. Cover all four tidy types (ar, car, aar, model) at least at the "does not error + NA preserved" level.

    Do NOT write any acceptance-criteria phrase verbatim into a code comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_export.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>export_results() or tidy() errors on NA abnormal returns; an interior NA blanks the whole CAR tail; the AR column shows 0 where the source was NA; or valid-input output changed.</fails_when>
  </verify>
  <done>export/tidy tolerate NA abnormal returns across all four tidy types; CAR uses coalesce; AR preserves NA; test_export.R passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cross_sectional_regression() singular/collinear guard + sandwich message->warning (PIPELINE-03, EXTERNAL-03 pipeline half)</name>
  <files>R/cross_sectional.R, tests/testthat/test_cross_sectional.R</files>
  <read_first>
    - R/cross_sectional.R lines 28-89 (already in context) — the bare `lm()` at 56, `sandwich::vcovHC` at 61, `message()` at 75.
    - 03-RESEARCH.md section 3 — the fix pattern (tryCatch around lm() and vcovHC, rank-deficiency check `fit$rank < length(coef(fit))`, NA-SE fallback).
  </read_first>
  <behavior>
    - A collinear/singular design (e.g., two perfectly correlated regressors) returns an es_cross_sectional object whose coefficient table has NA std.error/statistic/p.value for the aliased terms and emits exactly one warning about rank-deficiency / singular design — NO "system is computationally singular" crash from vcovHC.
    - `robust=TRUE` with sandwich mocked absent emits a `warning()` (not message()) that NAMES the lost capability (robust/HC SEs) and the OLS fallback; the function still returns a valid object with OLS SEs.
    - A well-conditioned design with sandwich present returns byte-identical coefficients/SEs to before.
  </behavior>
  <action>
    Wrap `stats::lm(reg_formula, data = merged)` in tryCatch; on error, warning(call.=FALSE) naming cross_sectional_regression and the lm failure, then `return(NULL)`. After a successful fit, compute `rank_deficient <- fit$rank < length(stats::coef(fit))`; if TRUE, emit one warning(call.=FALSE) naming rank-deficiency. Wrap the `sandwich::vcovHC(fit, type="HC1")` call in tryCatch; on error return NULL from the handler with a warning naming the singular design, and set `se <- if (!is.null(vcov_hc)) sqrt(diag(vcov_hc)) else rep(NA_real_, length(stats::coef(fit)))` so the coef_tbl carries NA SEs/statistic/p.value instead of crashing. Upgrade the `message(...)` at line 75 to `warning(...)` naming the lost capability (cluster/robust SEs unavailable) and the documented OLS fallback.

    Add tests: (a) singular design (duplicate/collinear regressor) -> expect_warning + NA in the aliased coefficient row + no error; (b) sandwich absent via `testthat::local_mocked_bindings(requireNamespace = function(pkg, ...) pkg != "sandwich", .package = "EventStudy")` -> expect_warning naming robust SEs + valid object (per research Pitfall 3, scope the mock to .package="EventStudy"); (c) well-conditioned design -> coefficients unchanged vs a pre-change baseline.

    Do NOT embed any acceptance-criteria phrase in a code comment.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all("."); df <- as.data.frame(testthat::test_file("tests/testthat/test_cross_sectional.R", reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error))'</automated>
    <fails_when>A singular design crashes with a solve()/vcovHC error; NA coefficients are not produced; sandwich-absent path emits message() instead of warning(); or a well-conditioned regression's coefficients changed.</fails_when>
  </verify>
  <done>Singular/collinear designs return NA coefficients + warning; sandwich-absent emits a named warning; well-conditioned output unchanged; test_cross_sectional.R passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user data.frame -> prepare_event_study / cross_sectional | Malformed/degenerate numeric input (missing dates, collinear regressors, NA returns) crosses into internal statistical routines. This is the ONLY boundary; there is no network/auth/untrusted-code boundary in an internal statistical library. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | prepare_event_study() missing/duplicate event date | medium | mitigate | Route through .handle_degenerate(): strict errors naming the event; lenient warns + all-zero windows so the model layer degrades cleanly (Task 1). |
| T-03-02 | Tampering | export/tidy on NA abnormal returns | low | mitigate | Coalesce-guarded CAR cumsum; NA-safe if() branches retained; regression tests (Task 2). |
| T-03-03 | Denial of Service | cross_sectional_regression() singular/collinear design | medium | mitigate | tryCatch around lm()+vcovHC + rank-deficiency check -> NA coefficients + warning instead of a solve() crash (Task 3). |

No network endpoints, authentication, session, or access-control surfaces are introduced. Package-install threats do not apply: no npm/pip/cargo installs and no new R package dependency in this plan.
</threat_model>

<verification>
Full quick-suite for the three touched areas:
```
Rscript -e 'devtools::load_all("."); for (f in c("tests/testthat/test_prepare.R","tests/testthat/test_export.R","tests/testthat/test_cross_sectional.R")) { df <- as.data.frame(testthat::test_file(f, reporter="silent")); stopifnot(sum(df$failed)==0, all(!df$error)) }'
```
Whole-suite regression (existing ~1200 tests stay green):
```
Rscript -e 'df <- as.data.frame(devtools::test(reporter="silent")); stopifnot(sum(df$failed)==0, sum(df$error)==0)'
```
</verification>

<success_criteria>
- SC1: non-existent event date -> clear warning naming the event (lenient) / stop() naming the event (strict); no uninformative crash, no silently-empty result.
- SC2: export_results()/tidy() on NA abnormal returns -> valid output, NAs as NA/blank, no crash.
- SC3: collinear cross-sectional design -> NA coefficients + warning, no singular-matrix crash.
- EXTERNAL-03 (pipeline half): sandwich absent in cross_sectional -> named warning (upgraded from message()).
- Valid-input behavior byte-identical; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-pipeline-and-external-hardening/03-01-SUMMARY.md` when done.
</output>
